# ConvertCustomKerneltoFunction

接受一个自定义的内核并转换为一个打包的函数。如果给定的节点不是自定义内核或不能被转换，将返回一个空号。

目标是Optimus节点图

## 图示

![]($-20221218-20170924.png)

## Inputs

在。执行。

目标。Optimus节点图对象参考。

在自定义内核中。Optimus节点对象参考。  

## Outputs

出：执行。

返回值。Optimus节点对象参考。接受一个自定义的内核并转换为一个打包的函数。如果给定的节点不是自定义内核或不能被转换，则返回一个nullptr。

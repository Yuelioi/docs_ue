# GetStandardRenderableComponents

读取此水体在不被水网渲染时使用的原始组件列表（例如，指定WaterMeshOverride时使用的静态网格组件）。

目标是水体部分

## 图示

![]($-20221218-20363729.png)

## Inputs

目标。水体组件对象参考。  

## Outputs

返回值。原始组件对象引用数组。读取此水体在不被水网渲染时使用的原始组件列表（例如，指定WaterMeshOverride时使用的静态网格组件）。

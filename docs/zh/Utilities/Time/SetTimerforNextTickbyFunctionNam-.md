# SetTimerforNextTickbyFunctionNam-

设置一个定时器，在下一个刻度上执行一个委托。

目标是Kismet系统库

## 图示

![]($-20221218-15245943.png)

## Inputs

在。执行。

对象。对象参考。实现委托功能的对象。默认为自我（此蓝图）。

函数名称。字符串。委托函数名称。可以是一个K2函数或一个自定义事件。  

## Outputs

出：执行。

返回值。定时器手柄结构。定时器句柄，传递给其他定时器函数来操作这个定时器。

# GetEditorProperty

试图从给定的对象中检索一个命名的属性的值。

目标是Kismet系统库

## 图示

![]($-20221218-14260132.png)

## Inputs

在。执行。

对象。对象参考。你想检索一个属性值的对象。

财产名称。名称。要检索值的对象属性的名称。

## Outputs

出：执行。

属性值。通配符。检索到的属性值，如果找到的话。

成功？：布尔值。是否找到并正确检索了该属性值。

# AsTime_fromUTC

将传入的日期和时间转换为文本，使用给定的时区（默认为本地时区）格式化为时间。这将把给定的日期和时间从UTC转换成给定的时区（考虑到DST）。

目标是Kismet文本库

## 图示

![]($-20221218-15240415.png)

## Inputs

在日期时间。日期时间结构（按参考）。

在时区：字符串。  

## Outputs

返回值。文本。将传入的日期和时间转换为文本，格式为使用给定时区的时间（默认为本地时区）。这将把给定的日期和时间从UTC转换到给定的时区（考虑到DST）。

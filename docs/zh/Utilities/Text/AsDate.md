# AsDate

将传入的日期和时间转换为文本，使用不变的时区将其格式化为日期。这将使用给定的日期和时间，所以假定它已经在正确的时区。

目标是Kismet文本库

## 图示

![]($-20221218-15235801.png)

## Inputs

在日期时间。日期时间结构（按参考）。  

## Outputs

返回值。文本。将传入的日期和时间转换为文本，使用不变的时区格式化为日期。这将使用给定的日期和时间，所以假定它已经在正确的时区。

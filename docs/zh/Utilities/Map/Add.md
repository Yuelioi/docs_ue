# Add

向地图添加一个键和值。如果有东西已经使用了所提供的key，它将被新的值所覆盖。调用后，Key会被保证与Value相关联，直到地图的后续突变。

目标是蓝图地图库

## 图示

![]($-20221218-14265348.png)

## Inputs

在。执行。

目标地图。通配符到通配符的地图。要添加键和值的地图。

键。通配符（通过引用）。将用于查找值的键。

值。通配符（通过引用）。以后要检索的值。 

## Outputs

输出。执行。

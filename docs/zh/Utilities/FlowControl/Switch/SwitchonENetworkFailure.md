# SwitchonENetworkFailure

选择一个与输入值相匹配的输出

## 图示

![]($-20221218-14215899.png)

## Inputs

在。执行：执行。

选择。ENetworkFailure 枚举。 

## Outputs

Net驱动已经存在。Exec.

网络驱动程序创建失败。Exec.

网络驱动程序监听失败。Exec.

连接丢失。Exec.

连接超时。Exec.

接收到故障。执行.

过时的客户。执行。

过时的服务器。执行。

待定连接失败。执行。

网络指南不匹配。执行。

网络校验码不匹配。执行。

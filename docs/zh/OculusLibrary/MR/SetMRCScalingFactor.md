# SetMRCScalingFactor

设置MRC配置的缩放系数。这应该是一个正值，设置为与VR玩家棋子的缩放比例相同，这样游戏捕捉和摄像机视频才会对齐。

目标是Oculus MRFunction Library

## 图示

![]($-20221218-20154706.png)

## Inputs

在。执行。

缩放系数。Float（单精度）。  

## Outputs

出：执行。

返回值。布尔值。设置MRC配置的缩放系数。这应该是一个正值，设置为与VR播放器卒的缩放比例相同，以便游戏捕获和摄像机视频对齐。

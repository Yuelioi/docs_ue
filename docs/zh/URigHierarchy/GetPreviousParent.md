# GetPreviousParent

返回一个元素在reparent操作之前的上一个父元素。

目标是钻机等级制度

## 图示

![]($-20221218-21193205.png)

## Inputs

目标。Rig Hierarchy对象参考。

在关键。Rig 元素键结构（通过参考）。要请求旧的父元素的键。  

## Outputs

返回值。Rig Element Key Structure.

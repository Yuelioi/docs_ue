# IsLiveLinkSubjectEnabled

客户端是否有启用此名称的主题。只能启用1个同名的主题。在画面开始时，将对启用的主题进行快照。该快照决定了在该帧的持续时间内将使用哪个主题。

目标是Live Link蓝图库

## 图示

![]($-20221218-19452073.png)

## Inputs

在。执行。

主题名称。实时链接 主题名称结构。  

## Outputs

出：执行。

返回值。布尔值。客户端是否有启用此名称的主题。只能启用1个同名的主题。在帧开始时，将对启用的主体进行快照。该快照决定了在该帧的持续时间内将使用哪个主题。

# GetValueIntPoint

获取此属性的记录值为一个int点。如果该属性不是int点类型的，返回的值就没有意义。

目标是Datasmith属性捕获元素

## 图示

![]($-20221218-18380394.png)

## Inputs

在。执行。

目标。Datasmith属性捕捉元素对象参考。 

## Outputs

输出。输出：执行。

返回值。Int点结构。获取该属性的记录值，作为一个int点。如果该属性不是int点类型，则返回值没有意义。

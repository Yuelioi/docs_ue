# GetValueColor

获取此属性的记录值，作为一个颜色。如果该属性不是颜色类型的，返回的值就没有意义。

目标是Datasmith属性捕获元素

## 图示

![]($-20221218-18380039.png)

## Inputs

在。执行。

目标。Datasmith属性捕捉元素对象参考。 

## Outputs

输出。输出：执行。

返回值。颜色结构。获取该属性的记录值，作为一种颜色。如果该属性不是颜色类型的，返回的值就没有意义。

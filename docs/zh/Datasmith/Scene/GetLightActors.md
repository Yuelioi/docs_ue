# GetLightActors

用Datasmith场景中处于层次结构根层的LightActor创建一个数组。使用RemoveLightActor来修改Datasmith场景。

目标是Datasmith场景元素基础

## 图示

![]($-20221218-18400956.png)

## Inputs

在。执行。

目标。Datasmith场景元素基本对象参考。  

## Outputs

出：执行。

返回值。Datasmith灯光演员元素对象的数组参考。在Datasmith场景中创建一个数组，其中包括处于层次结构根层的LightActor。使用RemoveLightActor来修改Datasmith场景。

# CreateVariantSet

创建一个新的变体集并将其添加到父级变体集中

目标是Datasmith级别的变量集元素

## 图示

![]($-20221218-18394993.png)

## Inputs

在。执行。

目标。Datasmith级变体集元素对象参考。

在元素名称：名称。  

## Outputs

出：执行。

返回值。Datasmith变体集元素对象参考。创建一个新的变体集，并将其添加到父级变体集中。

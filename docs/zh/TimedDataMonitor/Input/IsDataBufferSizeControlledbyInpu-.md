# IsDataBufferSizeControlledbyInpu-

该通道是否支持与它的输入不同的缓冲区大小。

目标是定时数据监控子系统

## 图示

![]($-20221218-21130860.png)

## Inputs

在。执行。

目标。定时数据监控子系统对象参考。

Identifier:定时数据监控器输入标识符结构（通过参考）。  

## Outputs

出：执行。

返回值。布尔值。该通道是否支持与它的输入不同的缓冲区大小。

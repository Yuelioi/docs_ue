# GetChannelEvalua-_2

返回评估时间与最古老的样本之间的平均距离，以秒为单位。

目标是定时数据监控子系统

## 图示

![]($-20221218-21122855.png)

## Inputs

在。执行。

目标。定时数据监控子系统对象参考。

Identifier:定时数据监控通道标识符结构（通过参考）。  

## Outputs

出：执行。

返回值。浮点数（单精度）。返回评估时间和最旧样本之间的平均距离，以秒为单位。

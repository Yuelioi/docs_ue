# GetChannelDataBufferSize

如果该通道确实支持，则获得该通道当前的最大采样数。

目标是定时数据监控子系统

## 图示

![]($-20221218-21122341.png)

## Inputs

在。执行。

目标。定时数据监控子系统对象参考。

Identifier:定时数据监控通道标识符结构（通过参考）。  

## Outputs

出：执行。

返回值。整数。如果该通道确实支持，则获得该通道当前的最大采样数。

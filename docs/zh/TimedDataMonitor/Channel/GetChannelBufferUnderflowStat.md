# GetChannelBufferUnderflowStat

返回自上次重置以来，该输入端检测到的缓冲区下溢的数量。

目标是定时数据监控子系统

## 图示

![]($-20221218-21122078.png)

## Inputs

在。执行。

目标。定时数据监控子系统对象参考。

Identifier:定时数据监控通道标识符结构（通过参考）。  

## Outputs

出：执行。

返回值。整数。返回自上次重置以来该输入检测到的缓冲区内溢的数量。

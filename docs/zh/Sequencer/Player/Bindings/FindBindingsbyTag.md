# FindBindingsbyTag

检索所有被标记为指定名称的绑定。

目标是水平序列演员

## 图示

![]($-20221218-20510576.png)

## Inputs

目标。水平序列演员对象参考。

标签。名称。用来查找绑定的唯一标签名称。对象绑定可以在序列UI中通过人民币->标签...在树中的对象绑定上进行标记。

## Outputs

返回值。电影场景对象绑定ID结构的数组。一个数组，包含所有被标记为这个名字的绑定，可能是空的。

# FindTracksbyType

在给定的绑定中查找指定类型的所有轨道

目标是电影场景绑定扩展

## 图示

![]($-20221218-20541001.png)

## Inputs

在。执行。

绑定中。定序器绑定代理结构（通过参考）。要在其中寻找轨道的绑定。

轨迹类型。电影场景轨迹类参考。一个UMovieSceneTrack类的类型，指定要返回哪些类型的轨道。  

## Outputs

出：执行。

返回值。电影场景轨道对象引用数组。一个包含任何符合指定类型的轨道的数组。

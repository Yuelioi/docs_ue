# SetCustomTranslatedTextureNodeUi-

设置翻译后的纹理节点的唯一ID。这是对翻译器创建的节点的引用，需要这个节点来获取纹理的有效载荷。

目标是互换纹理工厂节点

## 图示

![]($-20221218-19354310.png)

## Inputs

在。执行。

目标：互换纹理工厂节点对象参考。

属性值。字符串。  

## Outputs

出：执行。

返回值。布尔值。设置翻译后的纹理节点的唯一ID。这是翻译器创建的节点的引用，需要这个节点来获取纹理的有效载荷。

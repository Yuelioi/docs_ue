# GetActorTimeDilation

获取ActorTimeDilation - 这可用于输入控制或Slomo的速度控制。我们不想在全局范围内调整输入，因为输入可以用于UI，而UI并不关心TimeDilation。

目标是演员

## 图示

![]($-20221218-17342817.png)

## Inputs

目标。Actor对象参考。 

## Outputs

返回值。浮点数（单精度）。获取ActorTimeDilation - 这可以用于输入控制或Slomo的速度控制。我们不想在全局范围内调整输入，因为输入可以用于UI，而UI并不关心TimeDilation。

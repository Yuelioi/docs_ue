# SetOperatorField

在2个输入字段之间计算一个操作

目标是操作员字段

## 图示

![]($-20221218-18595936.png)

## Inputs

目标。操作员字段对象参考。

字段大小。Float（单精度）。操作员字段的幅值。

左边的字段。字段节点基本对象参考。要处理的输入字段A。

右边字段。字段节点基本对象参考。要处理的输入字段B。

字段操作。EFieldOperationType枚举。你想在2个字段之间进行的数学运算的类型。

## Outputs

返回值。运算器字段对象参考。
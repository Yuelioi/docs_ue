# SelectElements

试图选择给定的元素。

目标是类型化的元素选择集

## 图示

![]($-20221218-21171296.png)

## Inputs

在。执行。

目标。类型化的元素选择集对象参考。

在元素句柄中。脚本类型的元素处理结构的数组。

在选择选项中。类型化的元素选择选项结构。  

## Outputs

出：执行。

返回值。布尔值。如果选择被改变，则为真，否则为假。

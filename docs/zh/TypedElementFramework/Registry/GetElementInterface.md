# GetElementInterface

获取给定句柄所支持的元素接口，如果没有对该接口的支持或句柄无效，则为空。

目标是类型化的元素注册表

## 图示

![]($-20221218-21164312.png)

## Inputs

目标。类型化的元素注册表对象参考。

在元素处理中。脚本类型的元素句柄结构（通过参考）。

在基础接口类型。接口界面。  

## Outputs

返回值。对象参考。获取给定句柄所支持的元素接口，如果没有对该接口的支持或句柄无效，则为空。

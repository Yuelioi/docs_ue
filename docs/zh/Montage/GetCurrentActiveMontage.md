# GetCurrentActiveMontage

在这个AnimInstance中获得一个当前的Active Montage。请注意，在同一时间可能有多个活动。这将只返回它找到的第一个活跃的。*

目标是Anim Instance

## 图示

![]($-20221218-20065406.png)

## Inputs

目标。Anim 实例对象参考。  

## Outputs

返回值。动画蒙太奇对象参考。获取当前AnimInstance中的一个Active Montage。请注意，在同一时间可能有多个活动。这将只返回它找到的第一个活跃的。*.

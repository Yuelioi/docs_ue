# SetLods

在输入阵列中包含的静态网格上生成LOD。 由输入阵列中包含的演员生成LOD。

目标是Dataprep操作库

## 图示

![]($-20221218-18355892.png)

## Inputs

在。执行。

选定的对象。对象引用数组。UObjects数组，用于处理...

缩减选项。静态网格缩减选项结构（通过引用）。关于如何在网格上生成LOD的选项。 

## Outputs

Out: 执行：执行。

修改的对象。对象引用的数组。

# GetTriggerType

改变该触发器对有多个触发器的动作的影响方式：。所有隐式触发器都必须触发，才能触发该动作。如果有任何显式触发器，必须至少有一个触发器触发该动作。

目标是输入触发器

## 图示

![]($-20221218-21155526.png)

## Inputs

目标：输入触发对象参考。  

## Outputs

返回值。ETriggerType枚举。改变该触发器对有多个触发器的动作的影响方式：。所有隐式触发器都必须触发才能触发该动作。如果有任何显式触发器，必须至少有一个触发器触发该动作。

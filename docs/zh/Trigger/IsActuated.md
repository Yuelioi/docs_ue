# IsActuated

  * 传入的值是否足够大，以至于对触发器有兴趣。

  * 这是一个辅助函数，实现了对启动阈值最明显的（>=）解释。





目标是输入触发器

## 图示

![]($-20221218-21155645.png)

## Inputs

目标：输入触发对象参考。

对于价值。输入行动价值结构（由参考）。  

## Outputs

返回值。布尔值。传入的值是否大到足以引起触发器的兴趣。这是一个辅助函数，实现了对启动阈值最明显的（>=）解释。

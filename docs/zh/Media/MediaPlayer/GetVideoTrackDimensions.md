# GetVideoTrackDimensions

获取指定视频轨道的当前尺寸。

目标是媒体播放器

## 图示

![]($-20221218-20005900.png)

## Inputs

目标。媒体播放器对象参考。

轨迹索引。整数。轨迹的索引，如果是选定的，则是INDEX_NONE。

格式索引。整数。音轨格式的索引，或者选定的音轨格式为INDEX_NONE。  

## Outputs

返回值。Int 点结构。视频尺寸（以像素为单位）.。

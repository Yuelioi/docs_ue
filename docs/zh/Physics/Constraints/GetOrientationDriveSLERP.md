# GetOrientationDriveSLERP

获取是否启用了角向斜拉驱动。只有当AngularDriveMode被设置为SLERP时才相关。

目标是约束实例蓝图库

## 图示

![]($-20221218-20270200.png)

## Inputs

在。执行。

访问器。Constraint Instance Accessor结构（通过ref）。用于查询的约束访问器。  

## Outputs

出：执行。

输出 启用SLERP。布尔值。表示是否应该启用SLERP驱动。只有在AngularDriveMode被设置为SLERP时才相关。

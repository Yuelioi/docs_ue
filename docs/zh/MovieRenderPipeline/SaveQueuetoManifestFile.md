# SaveQueuetoManifestFile

获取指定的队列，将其复制并写入磁盘的./Saved/MovieRenderPipeline/文件夹中。返回复制的队列。

目标是电影管道编辑器蓝图库

## 图示

![]($-20221218-20095413.png)

## Inputs

在。执行。

在管线队列中。电影管道队列对象参考。  

## Outputs

出：执行。

Out Manifest文件路径。字符串。

返回值。电影管道队列对象参考。取出指定的队列，将其复制并写入磁盘的./Saved/MovieRenderPipeline/文件夹中。返回复制的队列。

# GetPipelineMasterConfig

获取用于渲染这个镜头的主配置。它包含该镜头的全局设置，以及每个镜头的配置，这些配置可以包含它们自己的设置。

目标是电影管道

## 图示

![]($-20221218-20090545.png)

## Inputs

目标。电影管道对象参考。  

## Outputs

返回值。电影管道主配置对象参考。获取用于渲染此镜头的主配置。它包含了该镜头的全局设置，以及每个镜头的配置，这些配置可以包含它们自己的设置。

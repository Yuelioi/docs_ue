# AddNewLayer

创建并添加一个新的图层到瓦片地图。注意：这只对拥有自己瓦片地图的组件起作用（OwnsTileMap返回true），你不能修改独立的瓦片地图资产。

目标是纸质瓦片地图组件

## 图示

![]($-20221218-21021277.png)

## Inputs

在。执行。

目标。纸质瓦片地图组件对象参考。  

## Outputs

出：执行。

返回值。纸片图层对象参考。创建并添加一个新的图层到瓦片地图。注意：这只对拥有自己瓦片地图的组件有效（OwnsTileMap返回true），你不能修改独立的瓦片地图资产。

# ContainsEmitterType

如果这个系统包含一个pasesd类型的发射器，返回true。@ param TypeData - 要检查的发射器类型。必须是UParticleModuleTypeDataBase的一个子类。

目标是粒子系统

## 图示

![]($-20221218-20175677.png)

## Inputs

在。执行。

目标。粒子系统对象参考。

类型数据。对象类参考。  

## Outputs

出：执行。

返回值。布尔值。如果这个系统包含一个pasesd类型的发射器，返回true。@ param TypeData - 要检查的发射器类型。必须是UParticleModuleTypeDataBase的一个子类。

# ConfigSession2

配置 ASA 会话。这将在下一个会话开始时生效。

目标是Azure Spatial Anchors Library

## 图示

![]($-20221218-17554418.png)

## Inputs

在。执行。

会话配置。Azure Spatial Anchors会话配置结构（按参考）。Azure云签到的相关配置.

粗略的本地化设置。粗略的本地化设置结构。与在世界范围内定位设备有关的设置（如GPS）。

Log Verbosity: EAzureSpatialAnchorsLogVerbosity Enum。Azure Spatial Anchor api的记录口令。 

## Outputs

出场。执行：执行。

返回值。布尔值。（布尔值）如果会话配置被设置为真。

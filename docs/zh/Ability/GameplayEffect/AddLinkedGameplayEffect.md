# AddLinkedGameplayEffect

将LinkedGameplayEffect添加到SpecHandles中。LinkedGameplayEffectSpec将在成功应用SpecHandle时被应用。这将为你初始化LinkedGameplayEffect的Spec。返回到新的链接的规格，以备你想在其中添加更多的内容。

目标是能力系统蓝图库

## 图示

![]($-20221218-17312222.png)

## Inputs

在。执行。

Spec Handle。Gameplay Effect Spec Handle 结构。

链接的游戏效果。游戏效果类参考。 

## Outputs

Out: 执行：执行。

返回值。游戏效果规格手柄结构。将LinkedGameplayEffect添加到SpecHandles中。如果SpecHandle被成功应用，LinkedGameplayEffectSpec将被应用。这将为你初始化LinkedGameplayEffect的Spec。返回到新的链接的Spec，以备你想向它添加更多的内容。

# MakeDirectory

建立一个新的目录，并可选择子目录

目标是蓝图文件利用系统BPLibrary

## 图示

![]($-20221218-19002426.png)

## Inputs

在。执行。

路径。字符串。要制作的目录路径。

创建树。布尔值。如果为真，整个目录树将被创建，如果它不存在。否则，如果可能的话，只有最左边的目录会被创建。  

## Outputs

出：执行。

返回值。布尔值。如果目录被创建，则为true，否则为false。

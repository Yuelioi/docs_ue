# GetListItems

获取列表中所有项目的列表。注意，这些项目中的每一个只有在可见时才有相应的条目部件。使用GetDisplayedEntryWidgets来获取当前显示的部件。

目标是列表视图

## 图示

![]($-20221218-19441662.png)

## Inputs

目标。列表视图对象参考。  

## Outputs

返回值。对象引用的数组。获取列表中所有项目的列表。注意，这些项目中的每一个只有在可见时才有一个相应的条目部件。使用GetDisplayedEntryWidgets来获取当前显示的小部件。

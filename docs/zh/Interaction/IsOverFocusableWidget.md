# IsOverFocusableWidget

如果命中结果下的小组件是可聚焦的，则返回true。例如，Slate小组件，对于SupportedKeyboardFocus()，返回true。

目标是小工具互动组件

## 图示

![]($-20221218-19303198.png)

## Inputs

目标。小工具交互组件对象参考。  

## Outputs

返回值。布尔值。如果命中结果下的小部件是可聚焦的，则返回true。例如，Slate小部件，对于SupportedKeyboardFocus()，返回true。

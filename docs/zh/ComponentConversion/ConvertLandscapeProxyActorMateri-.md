# ConvertLandscapeProxyActorMateri-

转换景观代理演员材料

目标是Usd转换蓝图的背景

## 图示

![]($-20221218-18220723.png)

## Inputs

在。执行。

目标。Usd Conversion Blueprint Context 对象参考。

行为者。景观代理对象参考。

原始路径。字符串。

要烘烤的属性。属性条目结构的数组。

默认纹理尺寸。Int Point结构（通过引用）。

Textures Dir：目录路径结构（通过引用）。

时间代码。Float（单精度）。 

## Outputs

输出。执行：执行。

返回值。布尔值。转换景观代理演员材料。

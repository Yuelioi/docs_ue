# ConnectMaterialProperty

将一个材质表达式输出连接到一个材质属性输入（例如漫反射颜色、不透明度等）。

目标是材料编辑库

## 图示

![]($-20221218-19461041.png)

## Inputs

在。执行。

从表达式。材料表达式对象参考。进行连接的表达式。

从输出名称。字符串。从FromExpression输出的名称，用于建立连接。

属性。EMaterialProperty 枚举。输入材料的属性，以进行连接。  

## Outputs

出：执行。

返回值。布尔值。

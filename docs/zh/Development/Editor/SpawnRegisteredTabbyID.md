# SpawnRegisteredTabbyID

给定一个标签的ID，尝试找到一个匹配的标签生成器，然后生成一个标签。如果它能够找到一个匹配的标签生成器，返回true。

目标是编辑实用程序子系统

## 图示

![]($-20221218-18423446.png)

## Inputs

在。执行。

目标。编辑工具子系统对象参考。

新标签的ID。名称。  

## Outputs

出：执行。

返回值。布尔值。给定一个标签的ID，尝试找到一个匹配的标签生成器，然后生成一个标签。如果它能够找到一个匹配的标签页生成器，返回true。

# SetEmitterFixedBounds

设置发射器实例的固定边界，这优先于所有其他的边界。这个盒子应该是在本地空间而不是世界空间。一个默认的未初始化的盒子将清除固定边界并恢复到发射器的固定/动态边界。

目标是Niagara粒子系统组件

## 图示

![]($-20221218-20133115.png)

## Inputs

在。执行。

目标。尼亚加拉粒子系统组件对象参考。

发射器名称：名称。

局部界限。箱形结构。  

## Outputs

出：执行。

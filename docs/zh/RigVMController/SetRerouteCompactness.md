# SetRerouteCompactness

设置图中重路由节点的紧凑程度。这会引起RerouteCompactnessChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20435789.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点中。Rig VMNode对象参考。

显示为完整节点。布尔值。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置图中重路由节点的紧凑程度。这会引起RerouteCompactnessChanged修改事件。

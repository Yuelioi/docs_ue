# LocalizeFunctions

将一系列的函数声明复制到本图的本地函数库中。

目标是Rig VMController

## 图示

![]($-20221218-20424819.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在函数定义中。Rig VMLibrary节点对象引用的数组。

本地化依赖性的私有函数。布尔型。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。将Rig VMLibrary节点对象引用映射到Rig VMLibrary节点对象引用。将一系列的函数声明复制到这个图形的本地函数库中。

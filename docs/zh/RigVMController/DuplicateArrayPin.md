# DuplicateArrayPin

复制一个数组元素pin...这将导致一个PinArraySizeChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20423166.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在阵列元素引脚路径。字符串。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。字符串。复制一个数组元素Pin...这将导致一个PinArraySizeChanged修改事件。

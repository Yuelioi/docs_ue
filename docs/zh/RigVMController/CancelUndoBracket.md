# CancelUndoBracket

取消撤消括号/范围内的交易...这主要是对Python有用。这将导致一个UndoBracketCanceled修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20422329.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。  

## Outputs

出：执行。

返回值。布尔值。取消一个撤销括号/范围内的事务。这主要是对Python有用。这将导致一个UndoBracketCanceled修改事件。

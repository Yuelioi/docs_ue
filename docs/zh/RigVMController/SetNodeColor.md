# SetNodeColor

设置图形中一个节点的颜色。这将引起一个NodeColorChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20433999.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点中。Rig VMNode对象参考。

在色彩方面：线性色彩结构（由参考文献）。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置图形中一个节点的颜色。这将引起一个NodeColorChanged修改事件。

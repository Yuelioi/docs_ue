# AddEnumNode

在图中添加一个枚举节点。枚举节点可以用来表示图中的常量枚举值

目标是Rig VMController

## 图示

![]($-20221218-20415089.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在CPPT类型对象路径中。名称（通过引用）。

在位。矢量二维结构（按参考）。

在节点名称中。字符串。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。Rig VMEnum节点对象参考。在图中添加一个枚举节点。枚举节点可以用来表示图中的常量枚举值。

# SelectNodebyName

在图中通过名称选择一个单一的节点。这将导致一个NodeSelected / NodeDeselected修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20432598.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点名称中。名称（通过参考）。

选择。布尔型。

设置撤销重做：布尔值。  

## Outputs

出：执行。

返回值。布尔值。通过名称选择图中的一个节点。这将导致一个NodeSelected / NodeDeselected修改事件。

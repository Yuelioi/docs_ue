# SendFileasByteArray

通过WebRTC对等体连接数据通道发送指定的字节数组。扩展名和mime类型用于在前端重建文件

目标是像素流蓝图

## 图示

![]($-20221218-20293160.png)

## Inputs

在。执行。

字节数组。字节数组。将通过数据通道发送的原始数据。

Mime类型。字符串。文件的mime类型。用于在前端重建。

文件扩展名。字符串。文件扩展名。用于在前端重建文件。  

## Outputs

出：执行。

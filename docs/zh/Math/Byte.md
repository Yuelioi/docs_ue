# Byte

在这一页

  * 行动





## Actions

Modulo (A % B): 目标是Kismet数学库。

制作字面字节。创建一个字面的字节。目标是Kismet系统库。

Max (Byte).返回A和B的最大值：目标是Kismet数学库。

Max Of Byte Array.返回所有数组条目的最大值以及找到该条目的索引。如果提供的数组是空的，返回值为0，索引为-1。目标是Kismet数学库。

Min (Byte).返回A和B的最小值：目标是Kismet数学库。

Min Of Byte Array.返回所有数组项的最小值以及找到该值的索引。如果提供的数组是空的，返回值为0，索引为-1。目标是Kismet数学库。

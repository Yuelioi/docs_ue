# AddSelectedActorstoDataLayers

将选定的角色添加到数据层中。

目标是数据层编辑子系统

## 图示

![]($-20221218-18344507.png)

## Inputs

在。执行。

目标。数据层编辑子系统对象参考。

数据层。数据层对象引用的数组。一个有效的DataLayers的列表。 

## Outputs

输出。执行。

返回值。布尔值。如果至少有一个角色被添加，则为true。如果所有被选中的角色已经属于DataLayers，则返回false。

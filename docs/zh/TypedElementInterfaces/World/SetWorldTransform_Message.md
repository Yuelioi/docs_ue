# SetWorldTransform_Message

试图在其所有者世界中设置该元素的变换。

目标是类型化的元素世界界面

## 图示

![]($-20221218-21184173.png)

## Inputs

在。执行。

目标。对象参考。

在元素处理中。脚本类型的元素句柄结构（通过参考）。

在转化。转变（通过参考）。  

## Outputs

出：执行。

返回值。布尔值。试图在其所有者世界中设置该元素的变换。

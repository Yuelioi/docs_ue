# GetParentElement

获取这个元素的逻辑父级，如果有的话。 eg）一个组件可能会返回它的角色，或者一个静态网格实例可能会返回它的ISM组件。

目标是类型化的元素层次结构界面

## 图示

![]($-20221218-21173503.png)

## Inputs

在。执行。

目标。类型化的元素层次结构界面界面。

在元素处理中。脚本类型的元素句柄结构（通过参考）。

允许创建。布尔型。  

## Outputs

出：执行。

返回值。脚本类型的元素处理结构。获取该元素的逻辑父级，如果有的话。例如）一个组件可能会返回它的角色，或者一个静态网格实例可能会返回它的ISM组件。

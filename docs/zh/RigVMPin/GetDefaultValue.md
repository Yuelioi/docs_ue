# GetDefaultValue

返回Pin的默认值为一个字符串。注意，这个值是基于针脚的子针脚计算的。例如，对于一个FVector类型的Pin，默认值实际上是由X、Y和Z SubPins的默认值组成的。

目标是Rig VMPin

## 图示

![]($-20221218-20455150.png)

## Inputs

目标。Rig VMPin对象参考。  

## Outputs

返回值。字符串。返回Pin的默认值为一个字符串。注意，这个值是根据针脚的子针脚计算的。例如，对于一个FVector类型的Pin，默认值实际上是由X、Y和Z SubPins的默认值组成的。

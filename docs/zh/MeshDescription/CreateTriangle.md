# CreateTriangle

添加一个新的三角形到网格中并返回其ID。这也会制作一个封装的多边形，以及任何缺失的边。

目标是网状描述基

## 图示

![]($-20221218-20032208.png)

## Inputs

在。执行。

目标。网格描述 基本对象参考。

多边形组ID。多边形组ID结构。

顶点实例ID。顶点实例ID结构的数组。  

## Outputs

出：执行。

新的边缘ID。边缘ID结构的数组。

返回值。三角形ID结构。为网格添加一个新的三角形并返回其ID。这也会制作一个封装的多边形，以及任何缺失的边。

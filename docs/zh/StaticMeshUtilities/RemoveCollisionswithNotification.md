# RemoveCollisionswithNotification

移除静态网格的碰撞。这个方法复制了在网格编辑器中调用菜单项 "Collision > Remove Collision "时的做法。

目标是静态网格编辑器子系统

## 图示

![]($-20221218-21035338.png)

## Inputs

在。执行。

目标。静态网格编辑器子系统对象参考。

静态网格。静态网格对象参考。静态网状物，用于消除碰撞。

应用变化。布尔值。表示是否必须应用更改。

## Outputs

出：执行。

返回值。布尔值。一个布尔值，表示删除是否成功。

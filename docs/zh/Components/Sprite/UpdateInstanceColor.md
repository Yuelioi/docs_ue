# UpdateInstanceColor

更新指定实例的颜色。成功时返回True。

目标是纸上分组的精灵组件

## 图示

![]($-20221218-18273709.png)

## Inputs

在。执行。

目标。Paper Grouped Sprite Component对象参考。

实例索引。整数。

New Instance Color: Linear Color Structure.

Mark Render State Dirty（标记渲染状态脏）。Boolean（布尔）。 

## Outputs

输出。执行。

返回值。布尔值。更新指定实例的颜色。成功时返回True。

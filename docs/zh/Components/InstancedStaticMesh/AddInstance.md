# AddInstance

为这个组件添加一个实例。除非设置了bWorldSpace，否则变换将在这个组件的本地空间中给出。

目标是实例化的静态网格组件

## 图示

![]($-20221218-18243090.png)

## Inputs

在。执行。

目标：Instanced Static Mesh Component对象参考。

实例变换。变换（通过参考）。

世界空间。Boolean（布尔）。 

## Outputs

输出。执行。

返回值。整数。为这个组件添加一个实例。除非设置了bWorldSpace，否则变换将在这个组件的本地空间中给出。

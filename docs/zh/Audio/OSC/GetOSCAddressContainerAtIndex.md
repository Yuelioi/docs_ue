# GetOSCAddressContainerAtIndex

返回提供的'Index'处的OSC地址容器。如果索引超出范围，则返回空字符串。

目标是OSCManager

## 图示

![]($-20221218-18053538.png)

## Inputs

在。Exec.

地址。OSCAddress结构（通过引用）。

索引。整数。 

## Outputs

输出。执行：执行。

容器。字符串。返回提供的'Index'处的OSC地址容器。如果索引在界外，则返回空字符串。

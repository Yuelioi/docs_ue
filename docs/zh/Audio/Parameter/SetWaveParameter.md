# SetWaveParameter

设置与所提供的Wave名称相匹配的参数。为方便/向后兼容而提供，与SoundCues（参数接口支持任何对象，由查询它的系统决定是否是有效类型）。

目标是音频组件

## 图示

![]($-20221218-18071922.png)

## Inputs

在。执行。

目标。音频组件对象参考。

在名称。名称。要把波分配给的参数的名称。

在Wave中。Sound Wave Object Reference. 要设置的波值。 

## Outputs

输出。Exec.

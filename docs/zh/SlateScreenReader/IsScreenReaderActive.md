# IsScreenReaderActive

如果屏幕阅读器当前处于活动状态，并且屏幕阅读器用户可以使用无障碍服务，如文本转语音，则返回true。否则，它返回false。

目标是Slate屏幕阅读器引擎子系统

## 图示

![]($-20221218-20584665.png)

## Inputs

目标。石板屏幕阅读器引擎子系统对象参考。  

## Outputs

返回值。布尔值。如果屏幕阅读器当前处于活动状态，并且屏幕阅读器用户可以使用无障碍服务，如文本转语音，则返回true。否则，它返回假。

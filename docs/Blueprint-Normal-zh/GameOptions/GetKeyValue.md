# GetKeyValue

将一个键=值对分解成其键和值。

目标是游戏性的统计学

## 图示

![]($-20221218-19080195.png)

## Inputs

对。字符串。包含要拆分的一对的字符串。

## Outputs

钥匙。字符串。(out) Pair的关键部分。如果没有 = in 字符串将与Pair.相同。

价值。字符串。(out) 对子的值部分。如果没有=in字符串将为空。

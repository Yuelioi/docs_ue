# GetInputEvaluati-

返回评估时间和最新样本之间的距离的标准偏差，单位是秒。

目标是定时数据监控子系统

## 图示

![]($-20221218-21125648.png)

## Inputs

在。执行。

目标。定时数据监控子系统对象参考。

Identifier:定时数据监控器输入标识符结构（通过参考）。  

## Outputs

出：执行。

返回值。浮点数（单精度）。返回评估时间和最新样本之间距离的标准偏差，以秒为单位。

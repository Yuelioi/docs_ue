# GetDirectionatTime

给定一个从0到花键持续时间的时间，返回花键在那里的切线的单位方向向量。

目标是花键组件

## 图示

![]($-20221218-21002726.png)

## Inputs

目标。花键组件对象参考。

时间。浮点数（单精度）。

坐标空间。ESplineCoordinateSpace Enum.

使用恒定速度。布尔型。  

## Outputs

返回值。向量。给定一个从0到花键持续时间的时间，返回花键切线的单位方向向量。

# GetDatasmithUserDataKeysandValue-

获取关联值包含字符串的键和值，以匹配给定对象的Datasmith用户数据。

目标是Datasmith内容蓝图库

## 图示

![]($-20221218-18410094.png)

## Inputs

在。执行。

对象。对象参考。从中获取Datasmith用户数据的对象。

要匹配的字符串。字符串。值中要匹配的字符串。  

## Outputs

出：执行。

出键。名称的数组。输出键的数组，其相关值包含要匹配的字符串。

输出值。字符串的数组。输出与键相关的值数组。

# GetMeshes

创建一个包含Datasmith场景中所有网格的数组。使用CreateMesh -or- RemoveMesh来修改Datasmith场景。

目标是Datasmith场景元素基础

## 图示

![]($-20221218-18401302.png)

## Inputs

在。执行。

目标。Datasmith场景元素基本对象参考。  

## Outputs

出：执行。

返回值。Datasmith网格元素对象的数组。创建一个包含Datasmith场景中所有Mesh的数组。使用CreateMesh -or- RemoveMesh来修改Datasmith场景。

# GetAllLightActors

创建一个包含Datasmith场景中所有LightActor的数组，而不考虑层次结构。使用RemoveLightActor来修改Datasmith场景。

目标是Datasmith场景元素基础

## 图示

![]($-20221218-18395602.png)

## Inputs

在。执行。

目标。Datasmith场景元素基本对象参考。  

## Outputs

出：执行。

返回值。Datasmith灯光演员元素对象的数组参考。创建一个包含Datasmith场景中所有光线演员的数组，而不考虑层次结构。使用RemoveLightActor来修改Datasmith场景。

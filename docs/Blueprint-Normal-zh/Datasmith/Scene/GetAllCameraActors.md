# GetAllCameraActors

创建一个包含Datasmith场景中所有CameraActor的数组，而不考虑层次结构。使用CreateCameraActor -or- RemoveCameraActor来修改Datasmith场景。

目标是Datasmith场景元素基础

## 图示

![]($-20221218-18395221.png)

## Inputs

在。执行。

目标。Datasmith场景元素基础对象参考。

## Outputs

输出。输出：Exec。

返回值。Datasmith相机演员元素对象引用的数组。创建一个包含Datasmith场景中所有相机角色的数组，而不考虑层次结构。使用CreateCameraActor -or- RemoveCameraActor来修改Datasmith场景。
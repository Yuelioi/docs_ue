# GetValueQuat

获取此属性的记录值，作为一个四元数。如果该属性不是quat类型的，返回的值就没有意义。

目标是Datasmith属性捕获元素

## 图示

![]($-20221218-18380635.png)

## Inputs

在。Exec.

目标。Datasmith属性捕获元素的对象参考。 

## Outputs

输出。输出：执行。

返回值。Quat结构。获取该属性的记录值，作为一个夸特。如果该属性不是quat类型的，则返回的值没有意义。

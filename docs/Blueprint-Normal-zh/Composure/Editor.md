# Editor

在这个页面上 

  *行动





## Actions

打开合成选取器。Open Compositing Picker: Target is Compositing Picker Async Task.

设置编辑器颜色选取器显示图像。EDITOR ONLY - 指定采撷时要显示的中间图像（如果不设置，我们默认为最终输出的图像）。目标是空的Comp Shot。

Set Editor Color Picking Target（设置编辑器采色目标）。EDITOR ONLY - 指定采色的中间目标（如果不设置，我们默认为显示图像）。目标是空的Comp Shot。

# IsLayer

检查提供的层名的ULayer对象是否存在。

目标是分层子系统

## 图示

![]($-20221218-19375536.png)

## Inputs

在。执行。

目标。分层子系统对象参考。

层名。名称（通过引用）。要检索的ULayer对象的层的名称。  

## Outputs

出：执行。

返回值。布尔值。如果为真，找到了一个有效的ULayer对象；如果为假，没有找到一个有效的ULayer对象。

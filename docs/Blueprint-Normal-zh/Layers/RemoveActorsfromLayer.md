# RemoveActorsfromLayer

将演员从指定的层中移除。

目标是分层子系统

## 图示

![]($-20221218-19375995.png)

## Inputs

在。执行。

目标。分层子系统对象参考。

演员。演员对象引用数组。要从提供的图层中删除的演员。

层名。名称（按编号）。

更新统计资料。布尔型。  

## Outputs

出：执行。

返回值。布尔值。如果至少有一个演员被从该层移除，则为真。如果所有的演员都已经属于该层，则返回假。

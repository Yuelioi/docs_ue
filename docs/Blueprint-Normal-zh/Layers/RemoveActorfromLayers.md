# RemoveActorfromLayers

将提供的演员从指定的层中移除。

目标是分层子系统

## 图示

![]($-20221218-19375881.png)

## Inputs

在。执行。

目标。分层子系统对象参考。

演员。演员对象参考。要从提供的层中移除的演员。

图层名称。名称的数组。一个有效的图层名称列表。

更新统计资料。布尔型。  

## Outputs

出：执行。

返回值。布尔值。如果该演员至少从所提供的一个层中被移除，则为真。

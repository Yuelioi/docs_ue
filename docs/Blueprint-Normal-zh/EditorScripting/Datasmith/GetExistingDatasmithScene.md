# GetExistingDatasmithScene

从磁盘上打开一个现有的DatasmithScene资产文件。

目标是Datasmith场景元素

## 图示

![]($-20221218-18492136.png)

## Inputs

在。执行。

资产路径。字符串。DFatasmithScene资产的路径（例如，"/Game/MyAsset"）。

## Outputs

出：执行。

返回值。Datasmith场景元素对象参考。打开的DatasmithScene，它可以被修改并且可以被重新导入。
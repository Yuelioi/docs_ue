# CanExecuteCommands

检查给定集合中的命令是否可以被执行。这也将在全局范围内检查CanExecuteCommands。

目标是UICommands脚本子系统

## 图示

![]($-20221218-18483608.png)

## Inputs

目标。UICommands Scripting Subsystem Object Reference。

设置名称：名称。

## Outputs

返回值。布尔值。检查给定集合中的命令是否可以被执行。这也将在全局范围内检查CanExecuteCommands。
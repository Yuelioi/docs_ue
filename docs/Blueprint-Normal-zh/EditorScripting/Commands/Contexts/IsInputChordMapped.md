# IsInputChordMapped

检查给定的输入和弦是否已经被映射到给定的上下文中的一个命令。这包括没有通过子系统注册的命令。

目标是UICommands脚本子系统

## 图示

![]($-20221218-18482551.png)

## Inputs

目标。UICommands Scripting Subsystem Object Reference。

语境名称：名称。

输入和弦。输入和弦结构。

## Outputs

返回值。布尔值。检查给定的输入和弦是否已经映射到给定的上下文中的一个命令。这包括没有通过子系统注册的命令。
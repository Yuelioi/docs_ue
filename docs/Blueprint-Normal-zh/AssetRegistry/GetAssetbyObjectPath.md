# GetAssetbyObjectPath

获取指定对象路径的资产数据

目标是资产注册处

## 图示

![]($-20221218-18002922.png)

## Inputs

在。执行。

目标。资产注册处界面。

对象路径。名称。要查询的对象的路径。

Include Only on Disk Assets: 布尔值。如果为真，内存中的对象将被忽略。该调用将更快。 

## Outputs

输出。执行。

返回值。资产数据结构。资产数据；如果找不到对象，将是无效的。

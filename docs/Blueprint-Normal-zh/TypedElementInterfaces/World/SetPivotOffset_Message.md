# SetPivotOffset_Message

试图设置这个元素的局部空间偏移，应该加到它的枢轴位置。

目标是类型化的元素世界界面

## 图示

![]($-20221218-21183702.png)

## Inputs

在。执行。

目标。对象参考。

在元素处理中。脚本类型的元素句柄结构（通过参考）。

在支点偏移。向量（通过参考）。  

## Outputs

出：执行。

返回值。布尔值。试图设置该元素的局部空间偏移量，该偏移量应添加到其枢轴位置。

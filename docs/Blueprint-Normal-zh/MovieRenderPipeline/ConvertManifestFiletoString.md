# ConvertManifestFiletoString

加载指定的清单文件，并将其转换为 FString，以嵌入 HTTP REST 请求。与 SaveQueueToManifestFile 结合使用。

目标是电影管道编辑器蓝图库

## 图示

![]($-20221218-20081786.png)

## Inputs

在。执行。

在Manifest文件路径。字符串。  

## Outputs

出：执行。

返回值。字符串。加载指定的清单文件并将其转换为FString，以嵌入HTTP REST请求。与SaveQueueToManifestFile.结合使用。

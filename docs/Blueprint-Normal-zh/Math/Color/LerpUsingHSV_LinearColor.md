# LerpUsingHSV_LinearColor

以指定的Alpha值在两种颜色之间进行线性插值（Alpha=0时为A的100%，Alpha=1时为B的100%）。插值是在HSV色彩空间中进行的，采取最短的路径到达新颜色的色调。这比一般的插值能得到更好的结果，但成本要高得多。输入的颜色是在RGB空间，输出的颜色将是RGB。alpha值也将被插值。

目标是Kismet数学图书馆

## 图示

![]($-20221218-19472948.png)

## Inputs

A：线性颜色结构。作为线性RGBA插值的颜色和α。

B：线性颜色结构。将颜色和阿尔法插值为线性RGBA。

阿尔法。Float（单精度）。标度插值量（通常在0.0和1.0之间，包括在内）。  

## Outputs

返回值。线性颜色结构。线性RGB空间中的插值颜色，以及插值的alpha值。

# IsPawnControlled

检查这个角色目前是否被控制（这个角色有一个有效的控制器，对于远程客户端来说，这将是假的）。

目标是卒子

## 图示

![]($-20221218-20192700.png)

## Inputs

目标。卒子对象参考。  

## Outputs

返回值。布尔值。检查这个角色目前是否被控制（该角色有一个有效的控制器，对于远程客户端来说，这将是假的）。

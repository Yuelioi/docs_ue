# GetAttributeSignalFormats

返回一个函数名称和它们的数据类型图。活动模式的通道跨度范围之外的函数被忽略。

目标是DMX灯具补丁

## 图示

![]($-20221218-18433686.png)

## Inputs

目标。DMX灯具补丁对象参考。

## Outputs

返回值。DMXAttribute名称结构到EDMXFixtureSignalFormat枚举的映射。返回一个函数名称及其数据类型的映射。活动模式的通道跨度范围之外的函数被忽略。
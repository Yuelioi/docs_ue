# GetLinearVelocityDrive

获取线性速度驱动是否启用

目标是约束实例蓝图库

## 图示

![]($-20221218-20265972.png)

## Inputs

在。执行。

访问器。Constraint Instance Accessor结构（通过ref）。用于查询的约束访问器。  

## Outputs

出：执行。

Out Enable Drive X：布尔值。指示X轴的驱动器是否启用。

Out Enable Drive Y：布尔值。指示Y轴的驱动器是否启用。

Out Enable Drive Z：布尔值。表示是否启用Z轴的驱动。

# TransactObject

通知当前事务（如果有的话），这个对象即将被修改，应该被放入撤销缓冲区。注意：在内部，这将在给定的对象上调用修改，所以也将标记所有者包脏。注意：只在编辑器中可用。

目标是Kismet系统库

## 图示

![]($-20221218-21142076.png)

## Inputs

在。执行。

对象。对象参考。即将被修改的对象。

## Outputs

出：执行。

# SetAddress

设置服务器的地址和端口。如果服务器当前处于活动状态，则失败。

目标是OSCServer

## 图示

![]($-20221218-18062620.png)

## Inputs

在。执行。

目标。OSCServer 对象参考。

接收IPAddress。字符串。

端口：整数。 

## Outputs

输出。执行：执行。

返回值。布尔值。设置服务器的地址和端口。如果服务器当前处于活动状态，则失败。

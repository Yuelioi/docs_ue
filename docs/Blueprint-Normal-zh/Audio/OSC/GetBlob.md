# GetBlob

如果在范围内且类型匹配，将值设置为OSCMessage中提供的索引处的blob。

目标是OSCManager

## 图示

![]($-20221218-18053091.png)

## Inputs

在。执行。

消息。OSCMessage结构（通过引用）。

索引。整数。 

## Outputs

输出。Exec.

值。字节数组。

成功了。布尔值。将值设置为OSCMessage中提供的索引处的blob，如果在范围内且类型匹配。

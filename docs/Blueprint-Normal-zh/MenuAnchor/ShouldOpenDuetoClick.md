# ShouldOpenDuetoClick

如果我们应该因点击而打开菜单，则返回true。有时我们不应该这样做，如果刚刚关闭菜单的同一个MouseDownEvent因为刚好落在按钮上而要重新打开它。

目标是菜单锚

## 图示

![]($-20221218-20030542.png)

## Inputs

目标。菜单锚定对象参考。  

## Outputs

返回值。布尔值。如果我们应该因点击而打开菜单，则返回true。有时我们不应该这样做，如果刚刚关闭菜单的同一个MouseDownEvent因为刚好落在按钮上而要重新打开它。

# GetInputAnalogKeyState

返回给定键/按钮的模拟值。如果不支持模拟值，则返回1代表向下，0代表向上。

目标是玩家控制器

## 图示

![]($-20221218-19070580.png)

## Inputs

目标。玩家控制器对象参考。

钥匙。关键的结构。  

## Outputs

返回值。Float（单精度）。返回给定键/按钮的模拟值。如果不支持模拟值，则返回1代表向下，0代表向上。

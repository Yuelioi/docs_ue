# ToggleVRScoutingUI

在用户面前打开一个小部件UI。如果为空，则打开默认的VProd UI（通过 "虚拟侦察用户界面 "设置定义）。

目标是VPScouting子系统

## 图示

![]($-20221218-21314701.png)

## Inputs

在。执行。

目标。VPScouting子系统对象参考。

创建上下文。VREditor浮动的UICreation Context结构（通过参考）。  

## Outputs

出：执行。

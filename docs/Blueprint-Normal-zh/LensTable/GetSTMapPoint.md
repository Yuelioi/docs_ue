# GetSTMapPoint

通过给定的焦点和缩放来获取一个ST地图点，如果该点不存在则返回false。

目标是镜头文件

## 图示

![]($-20221218-19405367.png)

## Inputs

目标。镜头文件对象参考。

焦点。Float（单精度）。

在缩放中。Float（单精度）。  

## Outputs

输出STMap信息。STMap信息结构。

返回值。布尔值。通过给定的焦点和缩放来获取ST地图的一个点，如果该点不存在则返回false。

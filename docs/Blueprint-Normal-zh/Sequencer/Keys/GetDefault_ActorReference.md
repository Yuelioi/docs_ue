# GetDefault_ActorReference

获取此通道的默认值，当没有键存在时将被使用。只有当HasDefault()返回true时，才是有效的值。

目标是电影场景脚本的演员参考通道

## 图示

![]($-20221218-20485720.png)

## Inputs

目标。电影场景脚本演员参考通道对象参考。  

## Outputs

返回值。电影场景对象的绑定ID结构。获取该通道的默认值，当没有键存在时，将使用该值。只有当HasDefault()返回true时，才是有效的.值。

# SetNodeDescription

设置图中一个节点的功能描述。这会引起一个NodeDescriptionChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20434230.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点中。Rig VMCollapse Node Object Reference。

在描述中。字符串。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置图中一个节点的功能描述。这会引起一个NodeDescriptionChanged修改事件。

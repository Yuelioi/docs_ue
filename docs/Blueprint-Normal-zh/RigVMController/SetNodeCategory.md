# SetNodeCategory

设置图中节点的类别。这将导致一个NodeCategoryChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20433768.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点中。Rig VMCollapse Node Object Reference。

在类别中。绳子。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置图中节点的类别。这将导致一个NodeCategoryChanged修改事件。

# SetNodeSize

设置图形中一个节点的大小。这将导致一个NodeSizeChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20435070.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点中。Rig VMNode对象参考。

在尺寸上。矢量二维结构（按参考）。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置图中节点的大小。这将导致一个NodeSizeChanged修改事件。

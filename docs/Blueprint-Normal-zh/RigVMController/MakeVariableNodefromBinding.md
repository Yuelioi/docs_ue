# MakeVariableNodefromBinding

将一个绑定到一个变量节点

目标是Rig VMController

## 图示

![]($-20221218-20425063.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在销路：字符串。

在节点位置。矢量二维结构（通过参考）。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。将一个绑定到一个变量节点。

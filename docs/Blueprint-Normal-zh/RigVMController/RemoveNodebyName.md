# RemoveNodebyName

从图中删除一个节点，给定该节点的名称。这将导致一个NodeRemoved的修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20431366.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点名称中。名称（通过参考）。

设置撤销重做：布尔值。

递归。布尔型。

打印Python命令。布尔值。

重新链接引脚。布尔值。  

## Outputs

出：执行。

返回值。布尔值。从图中删除一个节点，给定该节点的名称。这将导致一个NodeRemoved修改事件。

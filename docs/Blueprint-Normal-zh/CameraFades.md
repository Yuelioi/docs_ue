# CameraFades

在这个页面上 

  *动作





## Actions

设置手动相机淡出。在给定的不透明度下开启相机渐变。不自动生成动画，允许用户自己生成动画。调用StopCameraFade来关闭渐变。目标是玩家相机管理器。

Start Camera Fade。摄像机淡入/淡出一种纯色。自动产生动画。目标是玩家相机管理器。

Stop Camera Fade（停止摄像机渐变）。停止摄像机渐变。目标是播放器摄像机管理器。

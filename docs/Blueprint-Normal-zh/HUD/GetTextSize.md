# GetTextSize

返回一个字符串的宽度和高度。

目标是HUD

## 图示

![]($-20221218-19162878.png)

## Inputs

目标：HUD对象参考。

文本。字符串。要画的字符串。

字体。字体对象参考。用于绘制文本的字体。如果为空，则选择默认字体。

规模。Float (single-precision).缩放倍数，用于控制文本的大小。  

## Outputs

Out Width: Float (single-precision).返回字符串的宽度（像素）。

输出高度。浮点数（单精度）。返回字符串的高度（像素）。

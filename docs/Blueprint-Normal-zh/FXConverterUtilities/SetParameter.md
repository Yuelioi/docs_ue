# SetParameter

在这个脚本转换上下文持有的脚本上设置一个参数。

目标是Niagara脚本转换语境

## 图示

![]($-20221218-19032796.png)

## Inputs

在。执行。

目标。Niagara Script Conversion Context Object Reference。

参数名称。字符串。目标参数名称。

参数输入。Niagara Script Conversion Context Input Object Reference。要在参数上设置的值。

在拥有编辑条件。布尔值。

在编辑条件值。布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置该参数是否成功。

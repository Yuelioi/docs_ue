# GetChildren

返回一个给定元素键的子元素

目标是钻机等级制度

## 图示

![]($-20221218-21185579.png)

## Inputs

目标。Rig Hierarchy对象参考。

在关键。Rig 元素键结构。检索子元素的键。

递归。布尔值。如果设置为 "真"，孙子辈也将被返回，等等。  

## Outputs

返回值。里格元素关键结构的数组。返回子元素。

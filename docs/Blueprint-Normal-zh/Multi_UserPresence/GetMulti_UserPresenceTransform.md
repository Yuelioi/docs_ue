# GetMulti_UserPresenceTransform

为指定的客户端点获取Presence Actor变换，如果没有找到客户端点，则获取其身份。

目标是多用户客户端统计学

## 图示

![]($-20221218-20102412.png)

## Inputs

在。执行。

客户端标识。指南结构（通过引用）。  

## Outputs

出：执行。

返回值。转换。为指定的客户端端点获取Presence Actor变换，如果没有找到客户端，则获取其身份。

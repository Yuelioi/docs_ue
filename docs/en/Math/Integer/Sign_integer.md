# Sign_integer

Sign (integer, returns -1 if A < 0, 0 if A is zero, and +1 if A > 0)

Target is Kismet Math Library

## 图示

![]($-20221218-19505558.png)

## Inputs

A: Integer.  

## Outputs

Return Value: Integer. Sign (integer, returns -1 if A < 0, 0 if A is zero, and +1 if A > 0).


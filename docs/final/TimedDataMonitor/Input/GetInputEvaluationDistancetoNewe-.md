# GetInputEvaluationDistancetoNewe-

返回评估时间和最新样本之间的最大平均距离，单位是秒。

目标是定时数据监控子系统

## 图示

![]($-20221218-21125892.png)

## Inputs

在。执行。

目标。定时数据监控子系统对象参考。

Identifier:定时数据监控器输入标识符结构（通过参考）。  

## Outputs

出：执行。

返回值。浮点数（单精度）。返回评估时间和最新样本之间的最大平均距离，单位是秒。

Returns the max average distance, in seconds, between evaluation time and newest sample

Target is Timed Data Monitor Subsystem

## 图示

![]($-20221218-21125892.png)

## Inputs

In: Exec.

Target: Timed Data Monitor Subsystem Object Reference.

Identifier: Timed Data Monitor Input Identifier Structure (by ref).  

## Outputs

Out: Exec.

Return Value: Float (single-precision). Returns the max average distance, in seconds, between evaluation time and newest sample.


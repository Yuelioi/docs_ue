# Contains

这个元素列表是否包含一个给定元素句柄的条目？

目标是类型化的元素列表库

## 图示

![]($-20221218-21162253.png)

## Inputs

元素列表。类型化的元素列表结构。

元素处理。脚本类型的元素句柄结构（通过参考）。  

## Outputs

返回值。布尔值。这个元素列表是否包含一个给定元素句柄的条目？

Does this element list contain an entry for the given element handle?

Target is Typed Element List Library

## 图示

![]($-20221218-21162253.png)

## Inputs

Element List: Typed Element List Structure.

Element Handle: Script Typed Element Handle Structure (by ref).  

## Outputs

Return Value: Boolean. Does this element list contain an entry for the given element handle?.


# IsStateBlendingOut

返回该节点所属的状态是否被混合掉了

目标是动画状态机库

## 图示

![]($-20221218-21025812.png)

## Inputs

更新上下文。Anim更新上下文结构（通过参考）。

节点。动画状态参考结构（通过参考）。  

## Outputs

返回值。布尔值。返回节点所属的状态是否被融合掉了。

Returns whether the state the node belongs to is blending out

Target is Animation State Machine Library

## 图示

![]($-20221218-21025812.png)

## Inputs

Update Context: Anim Update Context Structure (by ref).

Node: Animation State Reference Structure (by ref).  

## Outputs

Return Value: Boolean. Returns whether the state the node belongs to is blending out.


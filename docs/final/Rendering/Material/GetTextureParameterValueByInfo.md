# GetTextureParameterValueByInfo

获取当前的 MID 纹理参数值，使用 MPI（以允许访问层参数）。

目标是材料实例动态

## 图示

![](/uploads/projects/ue-bluprint/20221218-20375726.png)

## Inputs

在。执行。

目标。材料实例动态对象参考。

参数信息。材料参数信息结构（通过参考）。

## Outputs

出：执行。

返回值。纹理对象参考。获取当前 MID 纹理参数值，使用 MPI（允许访问图层参数）。

<hr>

Get the current MID texture parameter value, using MPI (to allow access to layer parameters)

Target is Material Instance Dynamic

## 图示

![](/uploads/projects/ue-bluprint/20221218-20375726.png)

## Inputs

In: Exec.

Target: Material Instance Dynamic Object Reference.

Parameter Info: Material Parameter Info Structure (by ref).

## Outputs

Out: Exec.

Return Value: Texture Object Reference. Get the current MID texture parameter value, using MPI (to allow access to layer parameters).

# ReadRenderTargetRawUVArea

令人难以置信的低效和缓慢的操作!使用由 UV[0,1]x[0,1]坐标定义的矩形，从渲染目标中原样读取一个区域的数值。

目标是 Kismet Rendering Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-20384663.png)

## Inputs

在。执行。

纹理渲染目标。纹理渲染目标 2D 对象参考。

地区。盒式二维结构。

正常化。布尔值。

## Outputs

出：执行。

返回值。线性颜色结构的数组。令人难以置信的低效和缓慢的操作!使用由 UV [0,1]x[0,1]坐标定义的矩形，从渲染目标中原样读取一个区域的数值。

<hr>

Incredibly inefficient and slow operation! Read an area of values as-is from a render target using a rectangle defined by UV [0,1]x[0,1] coordinates.

Target is Kismet Rendering Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-20384663.png)

## Inputs

In: Exec.

Texture Render Target: Texture Render Target 2D Object Reference.

Area: Box 2D Structure.

Normalize: Boolean.

## Outputs

Out: Exec.

Return Value: Array of Linear Color Structures. Incredibly inefficient and slow operation! Read an area of values as-is from a render target using a rectangle defined by UV [0,1]x[0,1] coordinates..

# HasPartialPath

如果当前PathFollowingComponent的路径是部分的（没有到达预期的目的地），则返回true。

目标是AIController

## 图示

![]($-20221218-17472398.png)

## Inputs

目标。AIController对象参考。 

## Outputs

返回值。布尔值。如果当前PathFollowingComponent的路径是部分的（没有到达期望的目的地），返回true。

Returns true if the current PathFollowingComponent's path is partial (does not reach desired destination).

Target is AIController

## 图示

![]($-20221218-17472398.png)

## Inputs

Target: AIController Object Reference.  

## Outputs

Return Value: Boolean. Returns true if the current PathFollowingComponent's path is partial (does not reach desired destination)..


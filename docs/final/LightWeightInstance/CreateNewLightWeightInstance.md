# CreateNewLightWeightInstance

返回一个新的轻量级实例的句柄，该实例代表一个 ActorClass 类型的对象。

目标是轻量级实例蓝图功能库

## 图示

![](/uploads/projects/ue-bluprint/20221218-19440559.png)

## Inputs

在。执行。

演员类。对象类参考。

改造：改造。

层。数据层对象参考。

世界。世界对象参考。

## Outputs

出：执行。

返回值。Actor Instance Handle 结构。返回一个新的轻量级实例的句柄，代表一个 ActorClass 类型的对象。

<hr>

Returns a handle to a new light weight instance that represents an object of type ActorClass

Target is Light Weight Instance Blueprint Function Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-19440559.png)

## Inputs

In: Exec.

Actor Class: Object Class Reference.

Transform: Transform.

Layer: Data Layer Object Reference.

World: World Object Reference.

## Outputs

Out: Exec.

Return Value: Actor Instance Handle Structure. Returns a handle to a new light weight instance that represents an object of type ActorClass.

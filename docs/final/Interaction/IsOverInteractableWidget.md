# IsOverInteractableWidget

如果命中结果下的小组件是互动的，则返回真，例如 Slate 小组件，对于 IsInteractable()返回真。

目标是小工具互动组件

## 图示

![](/uploads/projects/ue-bluprint/20221218-19303433.png)

## Inputs

目标。小工具交互组件对象参考。

## Outputs

返回值。布尔值。如果命中结果下的小组件是互动的，则返回真，例如 Slate 小组件，对于 IsInteractable()返回真。

<hr>

Returns true if a widget under the hit result is interactive. e.g. Slate widgets. that return true for IsInteractable().

Target is Widget Interaction Component

## 图示

![](/uploads/projects/ue-bluprint/20221218-19303433.png)

## Inputs

Target: Widget Interaction Component Object Reference.

## Outputs

Return Value: Boolean. Returns true if a widget under the hit result is interactive. e.g. Slate widgets. that return true for IsInteractable()..

# VectorGetAbsMax

求一个向量的最大绝对元素（abs(X), abs(Y)或abs(Z)）。

目标是Kismet数学图书馆

## 图示

![]($-20221218-19574939.png)

## Inputs

答：向量。  

## Outputs

返回值。浮点数（双精度）。查找一个向量的最大绝对元素（abs(X)、abs(Y)或abs(Z)）。

Find the maximum absolute element (abs(X), abs(Y) or abs(Z)) of a vector

Target is Kismet Math Library

## 图示

![]($-20221218-19574939.png)

## Inputs

A: Vector.  

## Outputs

Return Value: Float (double-precision). Find the maximum absolute element (abs(X), abs(Y) or abs(Z)) of a vector.


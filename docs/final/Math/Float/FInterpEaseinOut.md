# FInterpEaseinOut

在 A 和 B 之间插值，应用一个缓和的功能。Exp 控制曲线的程度。

目标是 Kismet 数学图书馆

## 图示

![](/uploads/projects/ue-bluprint/20221218-19494550.png)

## Inputs

A：浮点数（双精度）。

B：浮点数（双精度）。

阿尔法。Float (double-precision).

指数。Float（双精度）。

## Outputs

返回值。浮点数（双精度）。在 A 和 B 之间插值，应用一个缓和的函数。Exp 控制曲线的程度。

<hr>

Interpolate between A and B, applying an ease in/out function. Exp controls the degree of the curve.

Target is Kismet Math Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-19494550.png)

## Inputs

A: Float (double-precision).

B: Float (double-precision).

Alpha: Float (double-precision).

Exponent: Float (double-precision).

## Outputs

Return Value: Float (double-precision). Interpolate between A and B, applying an ease in/out function. Exp controls the degree of the curve..

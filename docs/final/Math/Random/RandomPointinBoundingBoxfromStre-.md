# RandomPointinBoundingBoxfromStre-

以第一个向量为原点，第二个向量为 AABB 的一半大小，返回指定包围盒内的一个随机点。

目标是 Kismet 数学图书馆

## 图示

![](/uploads/projects/ue-bluprint/20221218-19535286.png)

## Inputs

中心。矢量。

半尺寸。向量。

流。随机流结构（通过参考）。

## Outputs

返回值。矢量。以第一个向量为原点，第二个向量为 AABB 的一半大小，返回指定包围盒内的一个随机点。

<hr>

Returns a random point within the specified bounding box using the first vector as an origin and the second as the half size of the AABB.

Target is Kismet Math Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-19535286.png)

## Inputs

Center: Vector.

Half Size: Vector.

Stream: Random Stream Structure (by ref).

## Outputs

Return Value: Vector. Returns a random point within the specified bounding box using the first vector as an origin and the second as the half size of the AABB..

# GetShowsasFullNode

如果这个节点应该被显示为一个完整的节点，则返回 true；如果这个节点应该被显示为一个小圆圈，则返回 false。

目标是 Rig VMReroute 节点

## 图示

![](/uploads/projects/ue-bluprint/20221218-20463560.png)

## Inputs

目标。Rig VMReroute Node Object Reference。

## Outputs

返回值。布尔值。如果这个节点应该被显示为一个完整的节点，则返回 true；如果这个节点应该被显示为一个小圆圈，则返回 false。

<hr>

Returns true if this node should be shown as a full node,. or false if this node should be shown as a small circle.

Target is Rig VMReroute Node

## 图示

![](/uploads/projects/ue-bluprint/20221218-20463560.png)

## Inputs

Target: Rig VMReroute Node Object Reference.

## Outputs

Return Value: Boolean. Returns true if this node should be shown as a full node,. or false if this node should be shown as a small circle..

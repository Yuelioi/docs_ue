# SetOrientationDriveSLERP

启用/禁用角向斜拉驱动。只有当角度驱动模式被设置为SLERP时才相关。

目标是物理约束组件

## 图示

![]($-20221218-20213211.png)

## Inputs

在。执行。

目标。物理约束组件对象参考。

启用SLERP。布尔值。表示是否应该启用SLERP驱动。只有在AngularDriveMode被设置为SLERP时才相关。  

## Outputs

出：执行。

Enables/Disables the angular orientation slerp drive. Only relevant if the AngularDriveMode is set to SLERP

Target is Physics Constraint Component

## 图示

![]($-20221218-20213211.png)

## Inputs

In: Exec.

Target: Physics Constraint Component Object Reference.

Enable SLERP: Boolean. Indicates whether the SLERP drive should be enabled. Only relevant if the AngularDriveMode is set to SLERP.  

## Outputs

Out: Exec.


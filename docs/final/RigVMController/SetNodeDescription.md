# SetNodeDescription

设置图中一个节点的功能描述。这会引起一个 NodeDescriptionChanged 修改事件。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20434230.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在节点中。Rig VMCollapse Node Object Reference。

在描述中。字符串。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。布尔值。设置图中一个节点的功能描述。这会引起一个 NodeDescriptionChanged 修改事件。

<hr>

Sets the function description of a node in the graph.. This causes a NodeDescriptionChanged modified event.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20434230.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Node: Rig VMCollapse Node Object Reference.

In Description: String.

Setup Undo Redo: Boolean.

Merge Undo Action: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: Boolean. Sets the function description of a node in the graph.. This causes a NodeDescriptionChanged modified event..

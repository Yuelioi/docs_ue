# SelectNode

在图中选择一个单一的节点。这将导致一个 NodeSelected / NodeDeselected 修改事件。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20432486.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在节点中。Rig VMNode 对象参考。

选择。布尔型。

设置撤销重做：布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。布尔值。选择图形中的一个节点。这将导致一个 NodeSelected / NodeDeselected 修改事件。

<hr>

Selects a single node in the graph.. This causes a NodeSelected / NodeDeselected modified event.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20432486.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Node: Rig VMNode Object Reference.

Select: Boolean.

Setup Undo Redo: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: Boolean. Selects a single node in the graph.. This causes a NodeSelected / NodeDeselected modified event..

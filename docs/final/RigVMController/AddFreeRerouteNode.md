# AddFreeRerouteNode

添加一个免费的重路由节点

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20415322.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

显示为完整节点。布尔值。

在 CPPT 类型。字符串。

在 CPPT 类型对象路径中。名称（通过引用）。

是常数：布尔值。

在自定义小工具名称中。名称（通过参考）。

在默认值中。字符串。

在位。矢量二维结构（按参考）。

在节点名称中。字符串。

设置撤销重做：布尔值。

## Outputs

出：执行。

返回值。Rig VMReroute Node Object Reference。添加一个空闲的 Reroute Node。

<hr>

Adds a free Reroute Node

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20415322.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

Show as Full Node: Boolean.

In CPPType: String.

In CPPType Object Path: Name (by ref).

Is Constant: Boolean.

In Custom Widget Name: Name (by ref).

In Default Value: String.

In Position: Vector 2D Structure (by ref).

In Node Name: String.

Setup Undo Redo: Boolean.

## Outputs

Out: Exec.

Return Value: Rig VMReroute Node Object Reference. Adds a free Reroute Node.

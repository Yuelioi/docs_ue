# PromoteCollapseNodetoFunctionRef-

将一个折叠节点变成一个函数节点

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20425965.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在节点名称中。名称（通过参考）。

设置撤销重做：布尔值。

打印 Python 命令。布尔值。

在现有的函数定义路径中。字符串。

## Outputs

出：执行。

返回值。名称。将一个折叠节点变成一个函数节点。

<hr>

Turns a collapse node into a function node

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20425965.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Node Name: Name (by ref).

Setup Undo Redo: Boolean.

Print Python Command: Boolean.

In Existing Function Definition Path: String.

## Outputs

Out: Exec.

Return Value: Name. Turns a collapse node into a function node.

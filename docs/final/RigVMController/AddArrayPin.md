# AddArrayPin

将一个数组元素引脚添加到一个数组引脚的末端。这将导致一个 PinArraySizeChanged 修改事件。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20414740.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在 Array Pin Path 中。字符串。

在默认值中。字符串。

设置撤销重做：布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。字符串。将一个数组元素引脚添加到一个数组引脚的末端。这会引起 PinArraySizeChanged 修改事件。

<hr>

Adds an array element pin to the end of an array pin.. This causes a PinArraySizeChanged modified event.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20414740.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Array Pin Path: String.

In Default Value: String.

Setup Undo Redo: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: String. Adds an array element pin to the end of an array pin.. This causes a PinArraySizeChanged modified event..

# AddLocalVariable

在图中添加一个局部变量

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20420269.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在变量名称中。名称（通过参考）。

在 CPPT 类型。字符串。

在 CPPT 类型对象中。对象参考。

在默认值中。字符串。

设置撤销重做：布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。Rig VMGraph 变量描述结构。在图中添加一个局部变量。

<hr>

Add a local variable to the graph

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20420269.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Variable Name: Name (by ref).

In CPPType: String.

In CPPType Object: Object Reference.

In Default Value: String.

Setup Undo Redo: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: Rig VMGraph Variable Description Structure. Add a local variable to the graph.

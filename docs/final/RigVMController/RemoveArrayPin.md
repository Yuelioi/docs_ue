# RemoveArrayPin

从一个数组引脚中删除一个数组元素引脚。这将导致一个 PinArraySizeChanged 修改事件。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20430665.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在阵列元素引脚路径。字符串。

设置撤销重做：布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。布尔值。从一个数组引脚中删除一个数组元素引脚。这将导致一个 PinArraySizeChanged 修改事件。

<hr>

Removes an array element pin from an array pin.. This causes a PinArraySizeChanged modified event.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20430665.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Array Element Pin Path: String.

Setup Undo Redo: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: Boolean. Removes an array element pin from an array pin.. This causes a PinArraySizeChanged modified event..

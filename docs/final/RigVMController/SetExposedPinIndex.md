# SetExposedPinIndex

设置一个暴露的针脚的索引。这可以用来在节点上上下移动针脚。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20433074.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在引脚名称：名称（通过参考）。

在新的索引中。整数。

设置撤销重做：布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。布尔值。设置一个暴露的针脚的索引。这可以用来在节点上向上和向下移动针脚。

<hr>

Sets the index for an exposed pin. This can be used to move the pin up and down on the node.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20433074.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Pin Name: Name (by ref).

In New Index: Integer.

Setup Undo Redo: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: Boolean. Sets the index for an exposed pin. This can be used to move the pin up and down on the node..

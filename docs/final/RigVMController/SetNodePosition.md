# SetNodePosition

设置一个节点在图中的位置。这将导致一个 NodePositionChanged 修改事件。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20434725.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在节点中。Rig VMNode 对象参考。

在位。矢量二维结构（按参考）。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。布尔值。设置一个节点在图形中的位置。这将引起一个 NodePositionChanged 修改事件。

<hr>

Sets the position of a node in the graph.. This causes a NodePositionChanged modified event.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20434725.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Node: Rig VMNode Object Reference.

In Position: Vector 2D Structure (by ref).

Setup Undo Redo: Boolean.

Merge Undo Action: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: Boolean. Sets the position of a node in the graph.. This causes a NodePositionChanged modified event..

# AddCommentNode

在编辑过的图形中添加一个注释节点。注释可以用来注释图形。这将导致一个NodeAdded修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20414971.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在评论文本中。字符串。

在位。矢量二维结构（按参考）。

在尺寸上。矢量二维结构（按参考）。

在色彩方面：线性色彩结构（通过参考）。

在节点名称中。字符串。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。Rig VMComment Node Object Reference。在编辑过的图形中添加一个注释节点。注释可以用来对图形进行注释。这将导致一个NodeAdded修改事件。

Adds a Comment Node to the edited Graph.. Comments can be used to annotate the Graph.. This causes a NodeAdded modified event.

Target is Rig VMController

## 图示

![]($-20221218-20414971.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Comment Text: String.

In Position: Vector 2D Structure (by ref).

In Size: Vector 2D Structure (by ref).

In Color: Linear Color Structure (by ref).

In Node Name: String.

Setup Undo Redo: Boolean.

Print Python Command: Boolean.  

## Outputs

Out: Exec.

Return Value: Rig VMComment Node Object Reference. Adds a Comment Node to the edited Graph.. Comments can be used to annotate the Graph.. This causes a NodeAdded modified event..


# RemoveNodebyName

从图中删除一个节点，给定该节点的名称。这将导致一个 NodeRemoved 的修改事件。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20431366.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在节点名称中。名称（通过参考）。

设置撤销重做：布尔值。

递归。布尔型。

打印 Python 命令。布尔值。

重新链接引脚。布尔值。

## Outputs

出：执行。

返回值。布尔值。从图中删除一个节点，给定该节点的名称。这将导致一个 NodeRemoved 修改事件。

<hr>

Removes a node from the graph given the node's name.. This causes a NodeRemoved modified event.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20431366.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Node Name: Name (by ref).

Setup Undo Redo: Boolean.

Recursive: Boolean.

Print Python Command: Boolean.

Relink Pins: Boolean.

## Outputs

Out: Exec.

Return Value: Boolean. Removes a node from the graph given the node's name.. This causes a NodeRemoved modified event..

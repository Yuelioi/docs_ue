# SetNodeColorbyName

设置图形中一个节点的颜色的名称。这将引起一个 NodeColorChanged 修改事件。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20434111.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在节点名称中。名称（通过参考）。

在色彩方面：线性色彩结构（通过参考）。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

## Outputs

出：执行。

返回值。布尔值。设置图形中一个节点的颜色名称。这会引起一个 NodeColorChanged 修改事件。

<hr>

Sets the color of a node in the graph by name.. This causes a NodeColorChanged modified event.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20434111.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Node Name: Name (by ref).

In Color: Linear Color Structure (by ref).

Setup Undo Redo: Boolean.

Merge Undo Action: Boolean.

## Outputs

Out: Exec.

Return Value: Boolean. Sets the color of a node in the graph by name.. This causes a NodeColorChanged modified event..

# AddBranchNode

在图中添加一个分支节点。分支节点可以用来将执行分成多个分支，允许通过逻辑驱动行为。

目标是 Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20414857.png)

## Inputs

在。执行。

目标。Rig VMController 对象参考。

在位。矢量二维结构（按参考）。

在节点名称中。字符串。

设置撤销重做：布尔值。

打印 Python 命令。布尔值。

## Outputs

出：执行。

返回值。Rig VMBranch Node Object Reference。在图形中添加一个分支节点。分支节点可以用来将执行分成多个分支，允许通过逻辑驱动行为。

<hr>

Adds a branch node to the graph.. Branch nodes can be used to split the execution of into multiple branches,. allowing to drive behavior by logic.

Target is Rig VMController

## 图示

![](/uploads/projects/ue-bluprint/20221218-20414857.png)

## Inputs

In: Exec.

Target: Rig VMController Object Reference.

In Position: Vector 2D Structure (by ref).

In Node Name: String.

Setup Undo Redo: Boolean.

Print Python Command: Boolean.

## Outputs

Out: Exec.

Return Value: Rig VMBranch Node Object Reference. Adds a branch node to the graph.. Branch nodes can be used to split the execution of into multiple branches,. allowing to drive behavior by logic..

# RandomArrayItemfromStream

从指定的数组中获取一个随机项（使用随机流）。

目标是Kismet阵列库

## 图示

![]($-20221218-12171362.png)

## Inputs

目标阵列。通配符的阵列。的数组。

随机流。随机流结构（通过参考）。随机流。  

## Outputs

出来的项目。通配符。这个数组中的随机项目。

输出索引。整数。随机项目的索引（如果数组为空，将为-1）。

Gets a random item from specified array (using random stream)

Target is Kismet Array Library

## 图示

![]($-20221218-12171362.png)

## Inputs

Target Array: Array of Wildcards. The array.

Random Stream: Random Stream Structure (by ref). The random stream.  

## Outputs

Out Item: Wildcard. The random item from this array.

Out Index: Integer. The index of random item (will be -1 if array is empty).


# FrameNumbertoInteger

将一个FrameNumber转换为int32，以便在为方便而采取int32帧数的函数中使用。

目标是时间管理蓝图库

## 图示

![]($-20221218-15250379.png)

## Inputs

在帧号中。帧号结构（按参考）。  

## Outputs

返回值。整数。将一个FrameNumber转换为int32，以便在使用int32帧数的函数中使用，以方便。

Converts a FrameNumber to an int32 for use in functions that take int32 frame counts for convenience.

Target is Time Management Blueprint Library

## 图示

![]($-20221218-15250379.png)

## Inputs

In Frame Number: Frame Number Structure (by ref).  

## Outputs

Return Value: Integer. Converts a FrameNumber to an int32 for use in functions that take int32 frame counts for convenience..


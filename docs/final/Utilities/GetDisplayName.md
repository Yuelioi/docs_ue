# GetDisplayName

返回显示名称（或演员标签），作为调试帮助显示。注意：在编辑器的构建中，这是演员的标签。在非编辑器构建中，这是实际的对象名称。这个函数不应该被用来唯一地识别演员！。它没有被本地化，也不应该被用于向游戏的终端用户显示。

目标是 Kismet 系统库

## 图示

![](/uploads/projects/ue-bluprint/20221218-14260046.png)

## Inputs

对象。对象参考。

## Outputs

返回值。字符串。返回显示名称（或演员标签），作为调试帮助显示。注意：在编辑器的构建中，这是演员的标签。在非编辑器构建中，这是实际的对象名称。这个函数不应该被用来唯一地识别演员！。它没有被本地化，也不应该被用来显示给游戏的终端用户。

<hr>

Returns the display name (or actor label), for displaying as a debugging aid.. Note: In editor builds, this is the actor label. In non-editor builds, this is the actual object name. This function should not be used to uniquely identify actors!. It is not localized and should not be used for display to an end user of a game.

Target is Kismet System Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-14260046.png)

## Inputs

Object: Object Reference.

## Outputs

Return Value: String. Returns the display name (or actor label), for displaying as a debugging aid.. Note: In editor builds, this is the actor label. In non-editor builds, this is the actual object name. This function should not be used to uniquely identify actors!. It is not localized and should not be used for display to an end user of a game..

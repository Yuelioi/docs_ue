# GetBaseFilename

返回与 GetCleanFilename 相同的东西，但没有扩展名。

目标是蓝图路径库

## 图示

![](/uploads/projects/ue-bluprint/20221218-14274890.png)

## Inputs

在路径中。字符串。

删除路径。布尔值。

## Outputs

返回值。字符串。返回与 GetCleanFilename 相同的东西，但没有扩展名。

<hr>

Returns the same thing as GetCleanFilename, but without the extension

Target is Blueprint Paths Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-14274890.png)

## Inputs

In Path: String.

Remove Path: Boolean.

## Outputs

Return Value: String. Returns the same thing as GetCleanFilename, but without the extension.

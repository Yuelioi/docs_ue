# ProjectLogDir

返回引擎用于输出日志的目录。目前这不能是一个.ini设置，因为游戏在读取.ini文件之前就开始记录了。

目标是蓝图路径库

## 图示

![]($-20221218-14281931.png)

## Inputs

## Outputs

返回值。字符串。日志目录。

Returns the directory the engine uses to output logs. This currently can't. be an .ini setting as the game starts logging before it can read from .ini. files.

Target is Blueprint Paths Library

## 图示

![]($-20221218-14281931.png)

## Inputs

## Outputs

Return Value: String. log directory.


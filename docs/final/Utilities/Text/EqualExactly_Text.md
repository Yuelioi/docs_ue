# EqualExactly_Text

如果 A 和 B 在语言上相等（A == B），则返回真。

目标是 Kismet 文本库

## 图示

![](/uploads/projects/ue-bluprint/20221218-15240503.png)

## Inputs

答：文本（由裁判）。

B：文本（由裁判）。

## Outputs

返回值。布尔值。如果 A 和 B 在语言上相等（A == B），则返回真。

<hr>

Returns true if A and B are linguistically equal (A == B).

Target is Kismet Text Library

## 图示

![](/uploads/projects/ue-bluprint/20221218-15240503.png)

## Inputs

A: Text (by ref).

B: Text (by ref).

## Outputs

Return Value: Boolean. Returns true if A and B are linguistically equal (A == B)..

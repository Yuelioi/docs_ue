# GetAssetName

可选的，任何可以导入/导出资产的节点都应该设置我们将赋予资产的正确名称。如果该属性从未被设置，它将返回GetDisplayLabel。

目标是互换基础节点

## 图示

![]($-20221218-19360615.png)

## Inputs

目标：互换基础节点对象参考。  

## Outputs

返回值。字符串。可选的，任何可以导入/导出资产的节点都应该设置我们将赋予资产的正确名称。如果该属性从未被设置，它将返回GetDisplayLabel。

Optional, Any node that can import/export an asset should set the proper name we will give to the asset.. If the attribute was never set, it will return GetDisplayLabel.

Target is Interchange Base Node

## 图示

![]($-20221218-19360615.png)

## Inputs

Target: Interchange Base Node Object Reference.  

## Outputs

Return Value: String. Optional, Any node that can import/export an asset should set the proper name we will give to the asset.. If the attribute was never set, it will return GetDisplayLabel..


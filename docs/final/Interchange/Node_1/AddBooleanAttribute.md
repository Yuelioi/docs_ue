# AddBooleanAttribute

为这个节点添加一个布尔属性。如果该属性不存在或者我们不能添加它，则返回false。

目标是互换基础节点

## 图示

![]($-20221218-19355909.png)

## Inputs

在。执行。

目标：互换基础节点对象参考。

Node Attribute Key:字符串。

值。布尔值（通过引用）。  

## Outputs

出：执行。

返回值。布尔值。为这个节点添加一个布尔属性。如果该属性不存在或无法添加，则返回false。

Add a boolean attribute to this node. Return false if the attribute do not exist or if we cannot add it

Target is Interchange Base Node

## 图示

![]($-20221218-19355909.png)

## Inputs

In: Exec.

Target: Interchange Base Node Object Reference.

Node Attribute Key: String.

Value: Boolean (by ref).  

## Outputs

Out: Exec.

Return Value: Boolean. Add a boolean attribute to this node. Return false if the attribute do not exist or if we cannot add it.


# ShouldOpenDuetoClick

如果我们应该因点击而打开菜单，则返回 true。有时我们不应该这样做，如果刚刚关闭菜单的同一个 MouseDownEvent 因为刚好落在按钮上而要重新打开它。

目标是菜单锚

## 图示

![](/uploads/projects/ue-bluprint/20221218-20030542.png)

## Inputs

目标。菜单锚定对象参考。

## Outputs

返回值。布尔值。如果我们应该因点击而打开菜单，则返回 true。有时我们不应该这样做，如果刚刚关闭菜单的同一个 MouseDownEvent 因为刚好落在按钮上而要重新打开它。

<hr>

Returns true if we should open the menu due to a click. Sometimes we should not, if. the same MouseDownEvent that just closed the menu is about to re-open it because it. happens to land on the button.

Target is Menu Anchor

## 图示

![](/uploads/projects/ue-bluprint/20221218-20030542.png)

## Inputs

Target: Menu Anchor Object Reference.

## Outputs

Return Value: Boolean. Returns true if we should open the menu due to a click. Sometimes we should not, if. the same MouseDownEvent that just closed the menu is about to re-open it because it. happens to land on the button..

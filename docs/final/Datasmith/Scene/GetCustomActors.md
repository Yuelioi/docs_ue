# GetCustomActors

用Datasmith场景中的CustomActor创建一个数组，该数组位于层次结构的根层。使用RemoveCustomActor来修改Datasmith场景。

目标是Datasmith场景元素基础

## 图示

![]($-20221218-18400487.png)

## Inputs

在。执行。

目标。Datasmith场景元素基本对象参考。  

## Outputs

出：执行。

返回值。Datasmith自定义角色元素对象的数组参考。用Datasmith场景中处于层次结构根层的CustomActor创建一个数组。使用RemoveCustomActor来修改Datasmith场景。

Create an array with the CustomActor in the Datasmith scene that are at the root level of the hierarchy.. Use RemoveCustomActor to modify the Datasmith scene.

Target is Datasmith Scene Element Base

## 图示

![]($-20221218-18400487.png)

## Inputs

In: Exec.

Target: Datasmith Scene Element Base Object Reference.  

## Outputs

Out: Exec.

Return Value: Array of Datasmith Custom Actor Element Object References. Create an array with the CustomActor in the Datasmith scene that are at the root level of the hierarchy.. Use RemoveCustomActor to modify the Datasmith scene..


# GetMeshActors

在Datasmith场景中创建一个数组，其中包括处于层次结构根层的MeshActor。使用CreateMeshActor -or- RemoveMeshActor来修改Datasmith场景。

目标是Datasmith场景元素基础

## 图示

![]($-20221218-18401066.png)

## Inputs

在。执行。

目标。Datasmith场景元素基本对象参考。  

## Outputs

出：执行。

返回值。Datasmith Mesh Actor元素对象的数组参考。用Datasmith场景中处于层次结构根层的MeshActor创建一个数组。使用CreateMeshActor -or- RemoveMeshActor来修改Datasmith场景。

Create an array with the MeshActor in the Datasmith scene that are at the root level of the hierarchy.. Use CreateMeshActor -or- RemoveMeshActor to modify the Datasmith scene.

Target is Datasmith Scene Element Base

## 图示

![]($-20221218-18401066.png)

## Inputs

In: Exec.

Target: Datasmith Scene Element Base Object Reference.  

## Outputs

Out: Exec.

Return Value: Array of Datasmith Mesh Actor Element Object References. Create an array with the MeshActor in the Datasmith scene that are at the root level of the hierarchy.. Use CreateMeshActor -or- RemoveMeshActor to modify the Datasmith scene..


# GetNextZAxisClockwiseAdjacency

获取给定邻接点的Z轴上顺时针方向的下一个邻接点。例如，GetNextZAxisClockwiseAdjacency(Front)将返回右侧。向上或向下将返回原来的邻接点。

目标是波函数崩溃BPLibrary

## 图示

![]($-20221218-21325567.png)

## Inputs

相邻关系。EWaveFunctionCollapseAdjacency枚举。邻接方向。  

## Outputs

返回值。EWaveFunctionCollapseAdjacency Enum.

Get the next adjacency in clockwise direction on a Z-axis for a given adjacency.. For example GetNextZAxisClockwiseAdjacency(Front) will return Right.. Up or Down will return the original adjacency.

Target is Wave Function Collapse BPLibrary

## 图示

![]($-20221218-21325567.png)

## Inputs

Adjacency: EWaveFunctionCollapseAdjacency Enum. Adjacency direction.  

## Outputs

Return Value: EWaveFunctionCollapseAdjacency Enum.


# AddStringtoOSCMessage

将字符串值添加到 OSCMessage 的结尾。

目标是 OSCManager

## 图示

![](/uploads/projects/ue-bluprint/20221218-18051369.png)

## Inputs

在。执行。

消息。OSCMessage 结构（通过引用）。

值。字符串（通过引用）。

## Outputs

输出。执行：执行。

消息。OSCMessage 结构。将字符串值添加到 OSCMessage 的结尾。

<hr>

Adds string value to end of OSCMessage

Target is OSCManager

## 图示

![](/uploads/projects/ue-bluprint/20221218-18051369.png)

## Inputs

In: Exec.

Message: OSCMessage Structure (by ref).

Value: String (by ref).

## Outputs

Out: Exec.

Message: OSCMessage Structure. Adds string value to end of OSCMessage.

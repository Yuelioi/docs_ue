# CreateOSCClient

创建一个 OSC 客户端。如果 SendIPAddress 为空（或'0'），则尝试使用 LocalHost IP 地址。

目标是 OSCManager

## 图示

![](/uploads/projects/ue-bluprint/20221218-18052615.png)

## Inputs

在。执行。

发送 IPAddress。字符串。

端口: 整数。

客户端名称。字符串。

外层。对象参考。

## Outputs

输出。执行：执行。

返回值。OSCClient 对象参考。创建一个 OSC 客户端。如果 SendIPAddress 为空（或'0'），尝试使用 LocalHost IP 地址。

<hr>

Creates an OSC Client. If SendIPAddress left empty (or '0'), attempts to use. attempts to use LocalHost IP address.

Target is OSCManager

## 图示

![](/uploads/projects/ue-bluprint/20221218-18052615.png)

## Inputs

In: Exec.

Send IPAddress: String.

Port: Integer.

Client Name: String.

Outer: Object Reference.

## Outputs

Out: Exec.

Return Value: OSCClient Object Reference. Creates an OSC Client. If SendIPAddress left empty (or '0'), attempts to use. attempts to use LocalHost IP address..

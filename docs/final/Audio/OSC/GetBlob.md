# GetBlob

如果在范围内且类型匹配，将值设置为 OSCMessage 中提供的索引处的 blob。

目标是 OSCManager

## 图示

![](/uploads/projects/ue-bluprint/20221218-18053091.png)

## Inputs

在。执行。

消息。OSCMessage 结构（通过引用）。

索引。整数。

## Outputs

输出。Exec.

值。字节数组。

成功了。布尔值。将值设置为 OSCMessage 中提供的索引处的 blob，如果在范围内且类型匹配。

<hr>

Sets Value to blob at provided Index from OSCMessage if in bounds and type matches

Target is OSCManager

## 图示

![](/uploads/projects/ue-bluprint/20221218-18053091.png)

## Inputs

In: Exec.

Message: OSCMessage Structure (by ref).

Index: Integer.

## Outputs

Out: Exec.

Value: Array of Bytes.

Succeeded: Boolean. Sets Value to blob at provided Index from OSCMessage if in bounds and type matches.

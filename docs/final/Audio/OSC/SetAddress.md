# SetAddress

设置服务器的地址和端口。如果服务器当前处于活动状态，则失败。

目标是 OSCServer

## 图示

![](/uploads/projects/ue-bluprint/20221218-18062620.png)

## Inputs

在。执行。

目标。OSCServer 对象参考。

接收 IPAddress。字符串。

端口：整数。

## Outputs

输出。执行：执行。

返回值。布尔值。设置服务器的地址和端口。如果服务器当前处于活动状态，则失败。

<hr>

Set the address and port of server. Fails if server is currently active.

Target is OSCServer

## 图示

![](/uploads/projects/ue-bluprint/20221218-18062620.png)

## Inputs

In: Exec.

Target: OSCServer Object Reference.

Receive IPAddress: String.

Port: Integer.

## Outputs

Out: Exec.

Return Value: Boolean. Set the address and port of server. Fails if server is currently active..

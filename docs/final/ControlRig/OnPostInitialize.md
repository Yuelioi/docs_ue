# OnPostInitialize

On Post Initialize Delegate:. Event fired after this component's ControlRig is initialized

<hr>

On Post Initialize Delegate:. Event fired after this component's ControlRig is initialized

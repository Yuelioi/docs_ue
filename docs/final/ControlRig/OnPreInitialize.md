# OnPreInitialize

On Pre Initialize Delegate:. Event fired just before this component's ControlRig is initialized


On Pre Initialize Delegate:. Event fired just before this component's ControlRig is initialized


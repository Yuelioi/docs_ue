# MakeInputActionValue

从X、Y、Z建立一个ActionValue，继承现有ActionValue的类型。忽略所提供的值类型未使用的轴值。注：旨在用于输入修改器修改原始重载，以修改现有的输入动作值。

目标是增强型输入库

## 图示

![]($-20221218-19271694.png)

## Inputs

X：浮点数（单精度）。

Y：浮点数（单精度）。

Z：浮点数（单精度）。

匹配值类型。输入行动值结构（通过参考）。  

## Outputs

返回值。输入动作值结构。从X、Y、Z建立一个动作值，继承现有动作值的类型。忽略所提供的值类型未使用的轴值。注释 用于输入修改器修改原始重载以修改现有的输入动作值。

Builds an ActionValue from X, Y, Z. Inherits type from an existing ActionValue. Ignores axis values unused by the provided value type.. Note: Intended for use in Input Modifier Modify Raw overloads to modify an existing Input Action Value.

Target is Enhanced Input Library

## 图示

![]($-20221218-19271694.png)

## Inputs

X: Float (single-precision).

Y: Float (single-precision).

Z: Float (single-precision).

Match Value Type: Input Action Value Structure (by ref).  

## Outputs

Return Value: Input Action Value Structure. Builds an ActionValue from X, Y, Z. Inherits type from an existing ActionValue. Ignores axis values unused by the provided value type.. @note Intended for use in Input Modifier Modify Raw overloads to modify an existing Input Action Value..


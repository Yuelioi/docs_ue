# Sign_integer64

Sign (integer64, returns -1 if A < 0, 0 if A is zero, and +1 if A > 0)

Target is Kismet Math Library

## 图示

![]($-20221218-19511127.png)

## Inputs

A: Integer64.  

## Outputs

Return Value: Integer64. Sign (integer64, returns -1 if A < 0, 0 if A is zero, and +1 if A > 0).


# VectorGetAbsMin

Find the minimum absolute element (abs(X), abs(Y) or abs(Z)) of a vector

Target is Kismet Math Library

## 图示

![]($-20221218-19575075.png)

## Inputs

A: Vector.  

## Outputs

Return Value: Float (double-precision). Find the minimum absolute element (abs(X), abs(Y) or abs(Z)) of a vector.


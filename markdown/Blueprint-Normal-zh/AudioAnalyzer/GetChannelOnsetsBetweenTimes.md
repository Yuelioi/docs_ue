# GetChannelOnsetsBetweenTimes

返回在开始和结束时间戳之间发生的onsets。

目标是Onset NRT

## 图示

![]($-20221218-18082479.png)

## Inputs

目标。Onset NRT对象参考。

在开始秒数。浮点数（单精度）。

在结束秒数中。Float (single-precision).

在通道中。整数。 

## Outputs

Out Onset Timestamps: Float（单精度）的数组。

Out Onset Strengths: Float (single-precision)s的数组。

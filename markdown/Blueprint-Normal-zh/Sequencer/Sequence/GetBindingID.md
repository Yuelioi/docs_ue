# GetBindingID

在一个序列中获取一个绑定的ID。注意：。产生的绑定只在应用于与此绑定相同的序列中的属性时有效。对于生活在不同子序列中的绑定，使用GetPortableBindingID。

目标是电影场景序列扩展

## 图示

![]($-20221218-20541116.png)

## Inputs

在。执行。

绑定中。定序器绑定代理结构（由参考）。  

## Outputs

出：执行。

返回值。电影场景对象的绑定ID结构。绑定的ID。

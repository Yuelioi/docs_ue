# Observe

观察阶段：。这个过程从最小熵瓦片中随机选择一个瓦片。然后随机选择该瓦片的有效选项

目标是波函数塌陷子系统

## 图示

![]($-20221218-21332397.png)

## Inputs

在。执行。

目标。波函数折叠子系统对象参考。

随机种子：整数。  

## Outputs

出：执行。

瓷砖。阵列的波函数塌陷瓦片结构。瓷砖的阵列（按参考）。

剩余的瓷砖。整数的数组。剩余瓦片的索引数组。半排序。最小熵的瓷砖在前面，其余的保持不排序（通过ref）。

观察队列。整数的映射到波函数折叠队列 元素结构。数组，用于存储需要检查其余选项在传播阶段是否受到影响的瓦片（通过ref）。

返回值。布尔值。

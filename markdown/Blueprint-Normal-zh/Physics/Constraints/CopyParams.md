# CopyParams

将一个约束的所有属性复制到另一个约束上

目标是约束实例蓝图库

## 图示

![]($-20221218-20232202.png)

## Inputs

在。执行。

访问器。Constraint Instance Accessor结构（通过引用）。要写入的约束访问器。

源访问器。Constraint Instance Accessor结构（通过ref）。读取的约束访问器。

保持位置。布尔值。是否保留原始约束的位置。

保持旋转。布尔值。是否保留原始约束的旋转。  

## Outputs

出：执行。

# Remove

从这个元素列表中移除给定的元素句柄，如果它在列表中的话。

目标是类型化的元素列表库

## 图示

![]($-20221218-21163508.png)

## Inputs

在。执行。

元素列表。类型化的元素列表结构。

元素句柄。脚本类型的元素句柄结构（通过参考）。  

## Outputs

出：执行。

返回值。布尔值。如果元素句柄被删除，则为真，如果不在列表中，则为假。

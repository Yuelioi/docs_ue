# SetSelectionfromList

试图对给定的元素进行选择。注意：等同于先调用ClearSelection然后再调用SelectElements，但在一个批次中发生。

目标是类型化的元素选择集库

## 图示

![]($-20221218-21171685.png)

## Inputs

在。执行。

选择集。类型化的元素选择集对象参考。

元素列表。类型化的元素列表结构。

选择选项。类型化的元素选择选项结构。  

## Outputs

出：执行。

返回值。布尔值。如果选择被改变，则为真，否则为假。

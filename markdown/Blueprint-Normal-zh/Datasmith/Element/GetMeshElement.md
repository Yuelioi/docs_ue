# GetMeshElement

获取与该角色相关的Datasmith MeshElement。该Mesh可以是对虚幻网格的直接引用。如果是这样的话，它将返回一个无效的MeshElement。

目标是Datasmith Mesh演员元素

## 图示

![]($-20221218-18372581.png)

## Inputs

在。执行。

目标。Datasmith Mesh Actor Element对象参考。 

## Outputs

输出。输出：Exec。

返回值。Datasmith网格元素对象参考。获取与角色相关的Datasmith MeshElement。该Mesh可以是对虚幻网格的直接引用。如果是这样的话，它将返回一个无效的MeshElement。

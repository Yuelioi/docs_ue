# GetPropertybyName

如果一个属性存在的话，通过它的名字来获取它

目标是Datasmith元数据元素

## 图示

![]($-20221218-18373268.png)

## Inputs

在。执行。

目标。Datasmith元数据元素对象参考。

在名称中。字符串。 

## Outputs

输出。执行：执行。

返回值。Datasmith键值属性对象参考。如果一个属性存在的话，通过它的名字获取该属性。

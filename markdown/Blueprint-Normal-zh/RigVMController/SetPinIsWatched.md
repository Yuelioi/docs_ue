# SetPinIsWatched

设置被监视的引脚（或不被监视）。这将导致一个PinWatchedChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20435551.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在销路：字符串。

是否被观看。布尔值。

设置撤销重做：布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置针脚被监视（或不被监视）。这将导致一个PinWatchedChanged修改事件。

# ResetPinDefaultValue

重置一个针脚的默认值，给定其针脚路径。这将导致一个PinDefaultValueChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20432366.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在销路：字符串。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。重置一个针脚的默认值，给定其针脚路径。这将导致一个PinDefaultValueChanged修改事件。

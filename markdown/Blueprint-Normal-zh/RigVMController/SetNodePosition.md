# SetNodePosition

设置一个节点在图中的位置。这将导致一个NodePositionChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20434725.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在节点中。Rig VMNode对象参考。

在位。矢量二维结构（按参考）。

设置撤销重做：布尔值。

合并撤消动作。布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。设置一个节点在图形中的位置。这将引起一个NodePositionChanged修改事件。

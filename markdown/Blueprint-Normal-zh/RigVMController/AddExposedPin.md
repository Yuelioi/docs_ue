# AddExposedPin

将一个暴露的引脚添加到由这个控制的图形中。

目标是Rig VMController

## 图示

![]($-20221218-20415207.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在引脚名称：名称（通过参考）。

在方向上。ERigVMPinDirection Enum.

在CPPT类型。字符串。

在CPPT类型对象路径中。名称（通过引用）。

在默认值中。字符串。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。名称。在此控制的图形中添加一个暴露的引脚。

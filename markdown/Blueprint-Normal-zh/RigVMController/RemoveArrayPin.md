# RemoveArrayPin

从一个数组引脚中删除一个数组元素引脚。这将导致一个PinArraySizeChanged修改事件。

目标是Rig VMController

## 图示

![]($-20221218-20430665.png)

## Inputs

在。执行。

目标。Rig VMController对象参考。

在阵列元素引脚路径。字符串。

设置撤销重做：布尔值。

打印Python命令。布尔值。  

## Outputs

出：执行。

返回值。布尔值。从一个数组引脚中删除一个数组元素引脚。这将导致一个PinArraySizeChanged修改事件。

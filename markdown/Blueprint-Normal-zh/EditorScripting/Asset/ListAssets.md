# ListAssets

返回在DirectoryPath中发现的所有资产的列表。

目标是编辑器资产库

## 图示

![]($-20221218-18472191.png)

## Inputs

在。执行。

目录路径。字符串。我们想要列出的资产的目录路径。

递归。布尔型。搜索将是递归的，将在子文件夹中寻找。

包括文件夹。布尔值。结果将包括文件夹名称。

## Outputs

出：执行。

返回值。字符串的数组。发现的资产的列表...
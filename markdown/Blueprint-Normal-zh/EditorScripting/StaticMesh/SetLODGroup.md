# SetLODGroup

为指定的静态网格设置LODGroup。

目标是静态网格编辑器子系统

## 图示

![]($-20221218-18541343.png)

## Inputs

在。执行。

目标。静态网格编辑器子系统对象参考。

静态网格。静态网格对象参考。要处理的网格。

LOD组。Name（名称）。要应用的LODGroup的名称。

Rebuild Immediately（立即重建）。布尔值。如果为true，立即重建静态网格。

## Outputs

输出。执行。

返回值。布尔值。成功。
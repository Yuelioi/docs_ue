# RenameDataLayer

将提供的DataLayer重命名为提供的新标签

目标是数据层编辑子系统

## 图示

![]($-20221218-18351441.png)

## Inputs

在。执行。

目标。数据层编辑子系统对象参考。

数据层。数据层对象参考。要重命名的数据层。

新的数据层标签。名称（通过参考）。要重命名的数据层的新标签。 

## Outputs

输出。执行：执行。

返回值。布尔值。

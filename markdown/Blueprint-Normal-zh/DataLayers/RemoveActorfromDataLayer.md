# RemoveActorfromDataLayer

从指定的数据层中删除一个角色。

目标是数据层编辑子系统

## 图示

![]($-20221218-18350618.png)

## Inputs

在。执行。

目标。数据层编辑子系统对象参考。

行为者。Actor对象参考。要从提供的数据层中移除的角色。

要移除的数据层。数据层对象参考。要移除角色的数据层。 

## Outputs

输出。执行：执行。

返回值。布尔值。如果演员被从数据层中移除，则为true。如果演员已经属于数据层，则返回false。

# LineTraceMultiForLidarPointCloud

沿着给定的线路进行碰撞追踪，并返回所有遇到的撞击，直到并包括第一个阻挡撞击。

目标是激光雷达点云蓝图库

## 图示

![]($-20221218-19433036.png)

## Inputs

原产地。向量。

方向。矢量。

半径。Float（单精度）。

仅可见。布尔型。  

## Outputs

点击率。激光雷达点云追踪命中结构的阵列。

返回值。布尔值。沿着给定的线路进行碰撞追踪，并返回所有遇到的撞击，包括第一个阻挡撞击。

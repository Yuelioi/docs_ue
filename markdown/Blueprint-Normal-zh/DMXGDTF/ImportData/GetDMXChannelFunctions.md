# GetDMXChannelFunctions

获取DMX通道的功能

目标是DMXImport GDTFDMXModes

## 图示

![]($-20221218-18451939.png)

## Inputs

目标。DMXImport GDTFDMXModes对象参考。

在模式中。DMXImport GDTFDMXMode结构（通过参考）。

## Outputs

返回值。DMXImport GDTFChannel函数结构的数组。获取DMXChannel函数。
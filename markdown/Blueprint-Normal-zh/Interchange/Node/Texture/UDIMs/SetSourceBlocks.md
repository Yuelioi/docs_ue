# SetSourceBlocks

设置源块。使用这个将强制纹理工厂将此纹理视为UDIM。

目标是互换纹理2DNode

## 图示

![]($-20221218-19340490.png)

## Inputs

在。执行。

目标：互换纹理2DNode对象参考。

在源区块中。整数到字符串的映射。构成整个纹理的块和它们的源图像。纹理必须是相同的格式，并使用相同的像素格式。在地图中插入的第一个块被用来确定接受的纹理格式和像素格式。  

## Outputs

出：执行。

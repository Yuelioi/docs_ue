# UpdatePostProcessSettingsforCapt-

更新提供的USceneCaptureComponent2D的PostProcessingSettings。成功时返回true。

目标是VCAM蓝图功能库

## 图示

![]($-20221218-21302637.png)

## Inputs

在。执行。

捕获组件。场景捕捉组件2D对象参考。

景深。浮点（单精度）。

FStop值。浮点数（单精度）。  

## Outputs

出：执行。

返回值。布尔值。更新提供的USceneCaptureComponent2D的后期处理设置。成功时返回true。

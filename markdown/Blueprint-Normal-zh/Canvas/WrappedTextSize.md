# WrappedTextSize

返回以屏幕空间为坐标的包裹的文本尺寸。

目标是Canvas

## 图示

![]($-20221218-18145082.png)

## Inputs

在。执行。

目标。Canvas对象参考。

渲染字体。字体对象参考。确定文本大小时使用的字体。如果它是空的，那么就使用默认的引擎字体。

Render Text（渲染文本）。字符串。决定文字大小的文本。 

## Outputs

输出。执行。

返回值。矢量2D结构。返回文本的屏幕空间大小。

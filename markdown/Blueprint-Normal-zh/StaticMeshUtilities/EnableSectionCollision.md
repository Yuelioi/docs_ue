# EnableSectionCollision

启用/禁用特定LOD的网格段碰撞。

目标是静态网格编辑器子系统

## 图示

![]($-20221218-21033068.png)

## Inputs

在。执行。

目标。静态网格编辑器子系统对象参考。

静态网格。静态网格对象参考。Static mesh to 启用/禁用碰撞。

启用碰撞。布尔值。如果碰撞被启用或未被启用。

LODIndex。整数。StaticMesh LOD的索引。

科目索引。整数。StaticMesh截面的索引。  

## Outputs

出：执行。

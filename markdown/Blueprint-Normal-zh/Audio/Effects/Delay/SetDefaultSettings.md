# SetDefaultSettings

设置对象的默认设置。这将更新UObject的默认设置（并将其标记为dirty），以及任何动态设置的设置。

目标是Submix Effect Delay Preset

## 图示

![]($-20221218-18040393.png)

## Inputs

在。执行。

目标。Submix Effect Delay Preset对象参考。

在设置中。Submix Effect Delay设置结构（通过参考）。 

## Outputs

输出。Exec.

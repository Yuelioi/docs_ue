# RemoveContainersfromOSCAddress

从有序的容器数组中移除容器的索引，直至容器的数量。

目标是OSCManager

## 图示

![]($-20221218-18062277.png)

## Inputs

在。执行。

地址。OSCAddress结构（通过引用）。

索引。整数。

计数。整数。 

## Outputs

输出。执行：执行。

地址。OSCAddress 结构。从有序的容器数组中移除容器，索引数最多为容器数。

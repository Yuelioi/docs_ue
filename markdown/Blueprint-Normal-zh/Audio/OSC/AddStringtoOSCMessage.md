# AddStringtoOSCMessage

将字符串值添加到OSCMessage的结尾。

目标是OSCManager

## 图示

![]($-20221218-18051369.png)

## Inputs

在。执行。

消息。OSCMessage结构（通过引用）。

值。字符串（通过引用）。 

## Outputs

输出。执行：执行。

消息。OSCMessage结构。将字符串值添加到OSCMessage的结尾。

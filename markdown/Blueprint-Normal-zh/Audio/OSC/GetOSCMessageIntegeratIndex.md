# GetOSCMessageIntegeratIndex

如果在范围内且类型匹配，将OSCMessage中提供的索引的值设置为整数。

目标是OSCManager

## 图示

![]($-20221218-18054983.png)

## Inputs

在。执行。

消息。OSCMessage结构（通过引用）。

索引。整数。 

## Outputs

输出。Exec.

值。整数。

成功了。布尔型。在OSCMessage中，如果在范围内且类型匹配，将值设置为所提供索引的整数。

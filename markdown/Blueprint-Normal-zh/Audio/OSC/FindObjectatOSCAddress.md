# FindObjectatOSCAddress

找到一个具有给定OSC地址的路径形式的对象，其中容器对应于路径文件夹，地址方法对应于对象的名称。只支持父对象。

目标是OSCManager

## 图示

![]($-20221218-18052867.png)

## Inputs

在。执行。

地址。OSCAddress结构（通过引用）。 

## Outputs

输出。Exec.

对象。对象参考。找到一个具有给定OSC地址的路径形式的对象，其中容器对应于路径文件夹，地址方法对应于对象的名称。只支持父对象。

# CreateOSCClient

创建一个OSC客户端。如果SendIPAddress为空（或'0'），则尝试使用LocalHost IP地址。

目标是OSCManager

## 图示

![]($-20221218-18052615.png)

## Inputs

在。执行。

发送IPAddress。字符串。

端口: 整数。

客户端名称。字符串。

外层。对象参考。 

## Outputs

输出。执行：执行。

返回值。OSCClient对象参考。创建一个OSC客户端。如果SendIPAddress为空（或'0'），尝试使用LocalHost IP地址。

# GetNodalOffsetPoint

通过给定的焦点和变焦获取一个节点偏移点，如果该点不存在则返回false。

目标是镜头文件

## 图示

![]($-20221218-19405150.png)

## Inputs

目标。镜头文件对象参考。

焦点。Float（单精度）。

在缩放中。Float（单精度）。  

## Outputs

输出结点偏移。节点偏移结构。

返回值。布尔值。通过给定的焦点和缩放来获取一个节点偏移点，如果该点不存在则返回false。

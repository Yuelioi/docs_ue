# GetRollatDistanceAlongSpline

给出沿该花键长度的一个距离，返回该花键在那里的滚动，单位是度。

目标是花键组件

## 图示

![]($-20221218-21004795.png)

## Inputs

目标。花键组件对象参考。

距离。浮点数（单精度）。

坐标空间。ESplineCoordinateSpace Enum.  

## Outputs

返回值。浮点数（单精度）。给出沿花键长度的一个距离，返回花键在那里的滚动，单位是度。

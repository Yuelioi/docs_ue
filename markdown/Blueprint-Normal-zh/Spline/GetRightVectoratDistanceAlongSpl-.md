# GetRightVectoratDistanceAlongSpl-

给出沿此花键长度的一个距离，返回一个单位方向的矢量，对应于该花键的右矢量。

目标是花键组件

## 图示

![]($-20221218-21004312.png)

## Inputs

目标。花键组件对象参考。

距离。浮点数（单精度）。

坐标空间。ESplineCoordinateSpace Enum.  

## Outputs

返回值。向量。给出一个沿此花键长度的距离，返回一个单位方向的向量，对应于花键的右边向量。

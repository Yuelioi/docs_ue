# ApplyTimeCorrection

假设所有的数据样本是在同一时间产生的，并将它们与当前平台的时间对齐

目标是定时数据监控子系统

## 图示

![]($-20221218-21121608.png)

## Inputs

在。执行。

目标。定时数据监控子系统对象参考。

Identifier:定时数据监控器输入标识符结构（通过参考）。

时间校正参数。定时数据监测器时间校正参数结构（由参考）。  

## Outputs

出：执行。

返回值。定时数据监控时间校正结果结构。假设所有的数据样本是在同一时间产生的，并与当前平台的时间对齐。

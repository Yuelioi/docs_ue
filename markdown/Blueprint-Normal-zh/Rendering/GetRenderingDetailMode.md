# GetRenderingDetailMode

获取r.DetailMode的钳制状态，见控制台变量帮助（允许扩展，不能在构造脚本中使用）。0：低，只显示细节模式为低或高的对象。1: 中等，显示所有细节模式为中等或以上的对象。2: 高，显示所有对象

目标是Kismet系统库

## 图示

![]($-20221218-20363390.png)

## Inputs

## Outputs

返回值。整数。获取r.DetailMode的钳制状态，见控制台变量帮助（允许扩展，不能在构造脚本中使用）。0：低，只显示细节模式为低或高的对象。1: 中等，显示所有细节模式为中等或以上的对象。2: 高，显示所有对象。

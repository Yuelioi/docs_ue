# AddTimedDisplay

添加一条信息，显示在这个世界的视口上。

目标是视口统计子系统

## 图示

![]($-20221218-21252979.png)

## Inputs

在。执行。

目标。视口统计子系统对象参考。

文本。文本。要显示的文本。

颜色：线性颜色结构。要显示的文本的颜色。

持续时间。Float（单精度）。文本在屏幕上停留的时间，如果是0，那么它将无限期地停留。

显示偏移。矢量二维结构（通过引用）。信息在显示时应使用的位置偏移。  

## Outputs

出：执行。

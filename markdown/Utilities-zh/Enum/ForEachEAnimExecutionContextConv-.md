# ForEachEAnimExecutionContextConv-

ForEach EAnimExecutionContextConversionResult

## 图示

![]($-20221218-13453385.png)

## Inputs

在。Exec.

跳过隐藏。布尔值。控制循环是否会跳过隐藏的枚举值。 

## Outputs

循环主体：Exec.

枚举值。EAnimExecutionContextConversionResult枚举。

已完成。循环体：Exec.Enum值：AnimExecutionContextConversionResult枚举。

# BreakSoundModulationMixValue

添加一个节点，将'SoundModulationMixValue'分解成其成员字段

## 图示

![]($-20221218-14430941.png)

## Inputs

声音调制混合值。声音调制混合值结构（按参考）。  

## Outputs

值。Float (single-precision).目标值：。调制器的目标值。

攻击时间（秒）。Float（单精度）。攻击时间：。从参数的默认值插值到混合值所需的时间（秒）。

释放时间（秒）。Float（单精度）。释放时间：。从当前的混合值插值到参数的默认值所需的时间（秒）。

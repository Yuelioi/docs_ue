# BreakDisplayClus-_20

添加一个节点，将'DisplayClusterConfigurationViewport_ColorGradingRenderingSettings'分解到其成员字段中。

## 图示

![]($-20221218-14340987.png)

## Inputs

Display Cluster Configuration Viewport Color Grading Rendering Settings（显示集群配置视口颜色分级渲染设置）。Display Cluster Configuration Viewport Color Grading Rendering Settings（显示集群配置视口颜色分级渲染设置）结构（通过参考）。 

## Outputs

曝光补偿。Float (single-precision). Auto Exposure Bias:（自动曝光偏差）。曝光补偿。

白平衡。显示集群配置视口颜色分级白平衡设置结构。White Balance:.白平衡 白平衡。

全局。显示集群配置视口颜色分级设置结构。全局：。全局调色。

阴影。显示集群配置视口调色设置结构。Shadows:（阴影）。阴影调色。

Shadows Max: Float（单精度）。Color Correction Shadows Max.

中调。Display Cluster Configuration Viewport Color Grading Settings Structure. Midtones:。中调子调色。

亮点。显示群组配置视口调色设置结构。高光：。高光调色。

高光最小值。Float（单精度）。Color Correction Highlights Min.

Misc: Display Cluster Configuration Viewport Color Grading Misc Settings Structure. Misc:. 突出调色的杂项设置。

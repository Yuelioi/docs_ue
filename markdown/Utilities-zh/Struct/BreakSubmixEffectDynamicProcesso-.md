# BreakSubmixEffectDynamicProcesso-

添加一个节点，将'SubmixEffectDynamicProcessorFilterSettings'分解到其成员字段中

## 图示

![]($-20221218-14435921.png)

## Inputs

Submix Effect动态处理器滤波器设置。Submix Effect动态处理器滤波器设置结构（按参考）。  

## Outputs

已启用。布尔型。启用：。是否启用了过滤器。

截止时间(Hz)。Float (single-precision).切断：。应用于关键信号的HPF的截止频率。

增益（D B）。Float (single-precision).增益Db：。应用于按键信号的滤波器架的增益。

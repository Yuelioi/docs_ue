# MakeTypedElementSelectionOptions

添加一个节点，从其成员中创建一个'TypedElementSelectionOptions'。

## 图示

![]($-20221218-15035114.png)

## Inputs

允许隐藏。布尔值。允许隐藏。

允许群组。布尔值。允许群组。

允许遗留通知。布尔型。允许遗留通知。

如果被锁定则警告。布尔值。锁定时发出警告。

子元素包容方法。ETypedElementChildInclusionMethod Enum.子元素包容方法。  

## Outputs

类型化的元素选择选项。类型化的元素选择选项结构。
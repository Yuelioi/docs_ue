# MakeRigMirrorSettings

添加一个节点，从其成员中创建一个 "RigMirrorSettings"。

## 图示

![]($-20221218-15002676.png)

## Inputs

镜像轴：EAxis Enum。镜像轴：。要镜像的轴。

要翻转的轴。EAxis Enum.Axis to Flip：.旋转时要翻转的轴。

搜索。字符串。搜索字符串：.要搜索的字符串。

替换。字符串。Replace String:.要替换搜索出现的字符串。  

## Outputs

钻机镜像设置。钻机镜像设置结构。

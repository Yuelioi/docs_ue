# BreakDisplayClusterConfiguration-_18

添加一个打破'DisplayClusterConfigurationRenderSyncPolicy'的节点到其成员字段中

## 图示

![]($-20221218-14343456.png)

## Inputs

Display Cluster Configuration Render Sync Policy（显示集群配置渲染同步策略）。Display Cluster Configuration Render Sync Policy结构（通过引用）。 

## Outputs

类型。字符串。类型:. 多态的实体类型。

参数。字符串到字符串的映射。参数:。通用参数映射。

# BreakMultiUserClientConfig

添加一个节点，将'MultiUserClientConfig'分解成其成员字段

## 图示

![]($-20221218-14403438.png)

## Inputs

多用户客户端配置。多用户客户端配置结构（按参考）。  

## Outputs

默认的服务器URL。字符串。默认的服务器URL。

默认会话名称：字符串。默认会话名称。

要恢复的默认会话。字符串。要恢复的默认会话。

验证模式。EMultiUserSourceValidationMode枚举。验证模式。

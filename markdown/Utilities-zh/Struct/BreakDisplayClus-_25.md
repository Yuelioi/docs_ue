# BreakDisplayClus-_25

添加一个打破'DisplayClusterConfigurationViewport_PerNodeColorGrading'的节点到其成员字段中

## 图示

![]($-20221218-14341431.png)

## Inputs

Display Cluster Configuration Viewport Per Node Color Grading。Display Cluster Configuration Viewport Per Node Color Grading 结构（通过参考）。 

## Outputs

启用每节点颜色分级。布尔值。是启用的：。启用指定节点的调色设置，并将其添加到 nDisplay 的调色堆中。

Include Entire Cluster Color Grading（包括整个集群调色）。Boolean（布尔）。Entire Cluster Color Grading:（整个集群调色）。可选择将根演员上指定的整个群组调色设置纳入 nDisplay 的调色堆栈，用于这些节点。

Include All Nodes Color Grading（包括所有节点调色）。布尔值。All Nodes Color Grading:（所有节点调色）。可选择在nDisplay的调色堆栈中为这些节点包括上面指定的所有节点调色设置。

Color Grading（色彩分级）。Display Cluster Configuration Viewport Color Grading Rendering Settings Structure. 颜色分级设置：. 颜色分级。

将颜色分级应用于节点。字符串阵列。将后期处理应用于对象：. 指定要应用这些调色设置的节点。

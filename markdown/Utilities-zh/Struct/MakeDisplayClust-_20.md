# MakeDisplayClust-_20

添加一个节点，从其成员中创建一个 "DisplayClusterConfigurationViewport_CustomPostprocessSettings"。

## 图示

![]($-20221218-14503691.png)

## Inputs

是否启用。布尔型。Is Enabled:.启用自定义后处理。

是一个框架。布尔型。是一帧：。对一帧应用后处理。

后期流程设置。后期处理设置结构。后处理设置：。自定义后处理设置。

混合重量。Float（单精度）。混合权重：。覆盖混合权重。  

## Outputs

显示群集配置视窗自定义后处理设置。显示群集配置视窗自定义后处理设置结构。

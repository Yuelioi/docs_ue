# BreakDMXImportGDTFLogicalChannel

添加一个打破'DMXImportGDTFLogicalChannel'的节点到其成员字段中。

## 图示

![]($-20221218-14351751.png)

## Inputs

DMXImport GDTFLogical Channel。DMXImport GDTFLogical Channel结构（通过引用）。 

## Outputs

属性。DMXImport GDTFAttribute结构。属性。

Snap: EDMXImportGDTFSnap枚举。捕捉。

主体。EDMXImportGDTFMaster Enum. 主站。

Mib Fade: Float (single-precision). Mib Fade.

DMXChange Time Limit: Float (single-precision). DMX变化时间限制。

通道功能。DMXImport GDTFChannel功能结构。通道功能

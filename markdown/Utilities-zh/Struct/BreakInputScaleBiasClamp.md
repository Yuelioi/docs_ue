# BreakInputScaleBiasClamp

添加一个节点，将'InputScaleBiasClamp'分解到其成员字段中。

## 图示

![]($-20221218-14382828.png)

## Inputs

输入刻度偏置钳。输入标度偏置钳结构（按参考）。  

## Outputs

地图范围。Boolean.地图范围。

钳制结果。布尔型。钳制结果。

Interp结果。布尔型。Interp结果。

在范围内。输入范围结构。在范围内。

输出范围。输入范围结构。输出范围。

规模。Float（单精度）。规模。

Bias: Float (single-precision).偏压。

夹子最小值。浮点数（单精度）。钳制最小值。

钳子最大值：浮点（单精度）。钳子的最大值。

Interp速度递增。Float（单精度）。Interp速度增加。

Interp速度递减。Float（单精度）。间歇期速度递减。

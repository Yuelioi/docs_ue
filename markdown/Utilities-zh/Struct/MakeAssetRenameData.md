# MakeAssetRenameData

添加一个节点，从其成员中创建一个'AssetRenameData'。

## 图示

![]($-20221218-14472180.png)

## Inputs

资产: 物体 虚弱的物体。资产：。被重命名的对象。

新包装路径。字符串。新包路径：。不含包名的新路径，即/Game/SubDirectory。

新名称。字符串。新名称：。新的包和资产名称，新的对象路径将是PackagePath/NewName.NewName。  

## Outputs

资产重命名数据。资产重命名数据结构。

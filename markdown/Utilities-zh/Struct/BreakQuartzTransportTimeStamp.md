# BreakQuartzTransportTimeStamp

添加一个节点，将 "QuartzTransportTimeStamp "分解到其成员字段中。

## 图示

![]($-20221218-14413638.png)

## Inputs

石英运输时间戳。石英运输时间戳结构（按参考）。  

## Outputs

Bars: 整数。Bars:.这个时钟当前所处的条形。

拍打。整数。Beat:.这个时钟当前所处的节拍。

节拍分数。Float (single-precision).Beat Fraction:.表示自上一次承受后所播放的时间的分数。

秒数。Float（单精度）。Seconds:.该时间戳发生的时间，以秒为单位。

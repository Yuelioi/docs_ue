# BreakInputClampConstants

添加一个节点，将'InputClampConstants'分解成其成员字段

## 图示

![]($-20221218-14382569.png)

## Inputs

输入钳制常数。输入钳制常数结构（通过参考）。  

## Outputs

钳制结果。布尔型。钳制结果。

Interp结果。布尔型。Interp结果。

夹子最小值。浮点数（单精度）。钳制最小值。

钳子最大值：浮点（单精度）。钳子的最大值。

Interp速度递增。Float（单精度）。Interp速度增加。

Interp速度递减。Float（单精度）。间歇期速度递减。

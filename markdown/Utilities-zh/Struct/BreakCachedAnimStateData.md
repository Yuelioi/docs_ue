# BreakCachedAnimStateData

添加一个节点，将'CachedAnimStateData'分解到其成员字段中。

## 图示

![]($-20221218-14320142.png)

## Inputs

缓存的动画状态数据。缓存的动画状态数据结构（通过引用）。 

## Outputs

状态机名称。名称。State Machine Name:.状态机名称。StateMachine的名称，状态在其中。

状态名称。名称。状态名称:. 要缓存的状态的名称。

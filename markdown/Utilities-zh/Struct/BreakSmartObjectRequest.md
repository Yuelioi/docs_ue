# BreakSmartObjectRequest

添加一个节点，将'SmartObjectRequest'分解成其成员字段

## 图示

![]($-20221218-14425099.png)

## Inputs

智能对象请求。智能对象请求结构（通过参考）。  

## Outputs

查询框。箱子结构。查询框：。定义搜索范围的方框。

过滤器。智能对象请求过滤器结构。过滤器：。用于过滤掉一些结果的结构（默认允许所有结果）。

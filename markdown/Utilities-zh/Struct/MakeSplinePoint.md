# MakeSplinePoint

添加一个节点，从其成员中创建一个 "SplinePoint"。

## 图示

![]($-20221218-15022218.png)

## Inputs

输入键。Float (single-precision).输入键。

位置。矢量。位置。

到达切线。向量。到达切线。

离开切线。向量。离开切线。

旋转： 旋转器。轮换。

规模：向量。规模。

类型。ESplinePointType枚举。类型。  

## Outputs

花键点。花键点结构。

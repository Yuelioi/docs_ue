# BreakLandmassFalloffSettings

添加一个节点，将'LandmassFalloffSettings'分解成其成员字段

## 图示

![]($-20221218-14385463.png)

## Inputs

Landmass Falloff设置。Landmass Falloff设置结构（按参考）。  

## Outputs

褪色模式：EBrushFalloffMode枚举。褪色模式。

滑移角度。Float（单精度）。漂移角度。

漂移宽度：浮点数（单精度）。衰减宽度。

边缘偏移。浮点数（单精度）。边缘偏移。

ZOffset。Float（单精度）。ZOffset.

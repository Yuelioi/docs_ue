# MakeDisplayClusterConfigurationI-_5

添加一个节点，从其成员中创建一个 "DisplayClusterConfigurationICVFX_StageSettings"。

## 图示

![]($-20221218-14505762.png)

## Inputs

启用内部信托基金：布尔值。启用内壳：。启用/禁用所有ICVFX相机的内侧地壳。

默认帧大小。显示群组配置ICVFX尺寸结构。默认帧大小：。默认的incameras RTT纹理大小。

光卡。显示群组配置 ICVFX光卡设置结构。光卡。

冻结视口。Boolean.Freeze Render Outer Viewports:冻结外部视口的渲染。冻结视口的渲染。这可以提高性能。

从整个集群中隐藏的内容。显示群集配置 ICVFX可见性列表结构。隐藏列表：。此处指定的内容将不会出现在 nDisplay 集群的任何地方。

从视口隐藏的内容。显示集群配置 ICVFX可见性列表结构。外视口隐藏列表：。此处指定的内容不会出现在 nDisplay 视口中，但可以出现在内部地壳中。

整个群组。显示群组配置视口整个群组颜色分级结构。整个 Cluster 颜色分级：.整个 Cluster 颜色分级。

每个视口的颜色分级。显示集群配置视口阵列 每视口颜色分级结构。每一视口调色：.在每个视口或视口组的基础上执行高级调色操作。

启用视口OCIO：布尔值。使用整体集群OCIOConfiguration：.启用对所有视口的OpenColorIO配置的应用。

所有视口的颜色配置。显示集群配置 OCIOConfiguration结构。所有视口 OCIOConfiguration:.将此OpenColorIO配置应用于所有视口。

每个视口的OCIO重写。显示集群配置OCIOProfile结构的阵列。每个视口的OCIOProfiles:.在每个视口或视口组的基础上应用一个OpenColorIO配置。  

## Outputs

显示群组配置ICVFX舞台设置。显示群组配置 ICVFX舞台设置结构。

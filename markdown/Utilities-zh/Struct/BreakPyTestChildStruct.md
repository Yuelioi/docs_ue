# BreakPyTestChildStruct

添加一个节点，将'PyTestChildStruct'分解成其成员字段

## 图示

![]($-20221218-14413018.png)

## Inputs

Py测试子结构。Py测试子结构的结构（通过参考）。  

## Outputs

Bool。布尔型。Bool.

Int: 整数。沟通。

浮点数。Float (single-precision).浮点数。

Enum: EPyTestEnum Enum.枚举。

字符串。字符串。

姓名：姓名名字。

文本。文本。文本。

字符串阵列。字符串的数组。字符串数组。

弦乐组。弦乐组合。弦乐组。

String Int Map:字符串到整数的映射。String Int Map.

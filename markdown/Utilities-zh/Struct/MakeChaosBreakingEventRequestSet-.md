# MakeChaosBreakingEventRequestSet-

添加一个节点，从其成员中创建一个'ChaosBreakingEventRequestSettings'。

## 图示

![]($-20221218-14482787.png)

## Inputs

结果的最大数量。整数。最大结果数：。要返回的最大结果数。

最小半径。Float（单精度）。最小半径：。结果的最小断裂半径阈值。

最小速度。Float（单精度）。最小速度：。结果的最小速度阈值。

最小质量。Float（单精度）。最小质量：。结果的最小质量阈值。

最大距离。Float（单精度）。最大距离：。结果的最大距离阈值。

排序方法。EChaosBreakingSortMethod Enum.排序方法：。用于对断裂事件进行排序的方法。  

## Outputs

破坏混沌的事件请求设置。破坏混沌事件请求设置结构。

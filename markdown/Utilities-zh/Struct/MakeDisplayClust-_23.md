# MakeDisplayClust-_23

添加一个节点，从其成员中创建一个 "DisplayClusterConfigurationViewport_PerViewportColorGrading"。

## 图示

![]($-20221218-14503949.png)

## Inputs

启用每个视口的颜色分级。布尔值。是启用的：。启用指定视口的调色设置，并将其添加到 nDisplay 的调色栈中。这将不会影响到内围的颜色。

包括整个群组的颜色分级。布尔型。Is Entire Cluster Enabled：.可选择将上面指定的整个群集调色设置纳入这些视口的nDisplay调色栈中。

颜色分级。显示集群配置 视口调色 渲染设置结构。颜色分级设置：.Color Grading.  

## Outputs

显示集群配置视口每视口颜色分级。显示集群配置 每个视口的颜色分级结构。

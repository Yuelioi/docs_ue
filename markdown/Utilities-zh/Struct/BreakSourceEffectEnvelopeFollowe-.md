# BreakSourceEffectEnvelopeFollowe-

添加一个节点，将'SourceEffectEnvelopeFollowerSettings'分解成其成员字段

## 图示

![]($-20221218-14432484.png)

## Inputs

源效果包络器设置。源效果包络器设置结构（按参考）。  

## Outputs

攻击时间。Float（单精度）。攻击时间：。包络器的攻击时间，单位是毫秒。

释放时间。Float（单精度）。释放时间：。包络器的释放时间，单位是毫秒。

峰值模式。EEnvelopeFollowerPeakMode枚举。峰值模式：。包络器的峰值模式。

是模拟模式。布尔值。是模拟模式：。包络器是否处于模拟模式。

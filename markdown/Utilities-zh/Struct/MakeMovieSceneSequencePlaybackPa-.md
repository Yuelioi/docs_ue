# MakeMovieSceneSequencePlaybackPa-

添加一个节点，从其成员中创建一个 "MovieSceneSequencePlaybackParams"。

## 图示

![]($-20221218-14581732.png)

## Inputs

框架。框架时间结构。框架。

时间。Float（单精度）。时间。

标记的框架。绳子。标记的框架。

位置类型。EMovieScenePositionType枚举。位置类型。

更新方法。EUpdatePositionMethod Enum.更新方法。

已经跳了。布尔型。已经跳槽。  

## Outputs

电影场景序列播放参数。电影场景序列播放参数结构。

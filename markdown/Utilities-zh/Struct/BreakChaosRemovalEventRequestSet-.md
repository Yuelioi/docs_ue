# BreakChaosRemovalEventRequestSet-

添加一个打破'ChaosRemovalEventRequestSettings'的节点到其成员字段中

## 图示

![]($-20221218-14322627.png)

## Inputs

Chaos Removal Event Request Settings。混沌清除事件请求设置结构（通过引用）。 

## Outputs

Max Number Of Results: 整数。最大结果数：。要返回的最大结果数。

最小质量。Float（单精度）。最小质量：。结果的最小质量阀值。

最大距离。Float (single-precision). 最大距离：。结果的最大距离阈值（如果破坏事件监听器上设置了位置）。

排序方法。EChaosRemovalSortMethod Enum。Sort Method:.排序方法。用来对清除事件进行排序的方法。

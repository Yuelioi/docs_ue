# MakeRigSpacePickerBakeSettings

添加一个节点，从其成员中创建一个 "RigSpacePickerBakeSettings"。

## 图示

![]($-20221218-15003453.png)

## Inputs

目标空间。钻机元素关键结构。目标空间。

开始帧。帧号结构。开始帧。

终端框架。框架编号结构。末端框架。

减少键。Boolean.缩减键。

误差。Float (single-precision).公差。  

## Outputs

钻机空间选取器烘烤设置。钻机空间选取器烘烤设置结构。

# BreakMovieSceneCameraShakeSource-

在其成员字段中添加一个打破'MovieSceneCameraShakeSourceTrigger'的节点

## 图示

![]($-20221218-14402380.png)

## Inputs

电影场景相机抖动源触发器。电影场景相机抖动源触发器结构（由参考）。  

## Outputs

抖动类。相机抖动基类参考。Shake Class:。要播放的相机抖动的类。

播放规模。Float（单精度）。播放规模：。影响抖动强度的标度。

播放空间。ECameraShakePlaySpace枚举。播放空间。

用户定义的游戏空间。旋转器。用户定义的游戏空间。

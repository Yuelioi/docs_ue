# MakeMotionTrajectorySettings

添加一个节点，从其成员中创建一个 "MotionTrajectorySettings"。

## 图示

![]($-20221218-14575545.png)

## Inputs

域。Bitmask 整数。域：。用于指定时间和/或距离域的轨迹掩码。默认值。时间域为2.0秒。

秒数。Float（单精度）。Seconds:.采样时间范围。

距离。Float（单精度）。距离：。采样距离的水平线。  

## Outputs

运动轨迹设置。运动轨迹设置结构。

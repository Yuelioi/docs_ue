# MakeDMXFixtureMatrix

添加一个节点，从其成员中创建一个 "DMXFixtureMatrix"。

## 图示

![]($-20221218-14512959.png)

## Inputs

单元属性。DMXFixture单元属性结构的阵列。单元属性。

第一个单元格通道。整数。第一个单元格通道。

X单元格。整数。XCells.

Y单元格。整数。YCells.

像素映射分布。EDMXPixelMappingDistribution 枚举。像素贴图分布。  

## Outputs

DMXFixture矩阵。DMX灯具矩阵结构。

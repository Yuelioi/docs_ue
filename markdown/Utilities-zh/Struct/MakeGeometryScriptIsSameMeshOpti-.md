# MakeGeometryScriptIsSameMeshOpti-

添加一个节点，从其成员中创建一个'GeometryScriptIsSameMeshOptions'。

## 图示

![]($-20221218-14540664.png)

## Inputs

检查连接性。布尔型。检查连接性。

检查边缘ID。布尔型。检查边缘ID。

检查规范。布尔型。检查规范。

检查颜色。Boolean.检查颜色。

检查UVs。布尔值。Check UVs.

检查组。布尔型。检查组。

检查属性。布尔型。检查属性。

Epsilon: 浮点数（单精度）。Epsilon.  

## Outputs

Geometry Script Is Same Mesh Options:Geometry Script Is Same 网格选项结构。

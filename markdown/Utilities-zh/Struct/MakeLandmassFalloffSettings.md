# MakeLandmassFalloffSettings

添加一个节点，从其成员中创建一个 "LandmassFalloffSettings"。

## 图示

![]($-20221218-14562666.png)

## Inputs

褪色模式：EBrushFalloffMode枚举。褪色模式。

滑移角度。Float（单精度）。漂移角度。

漂移宽度：浮点数（单精度）。衰减宽度。

边缘偏移。浮点数（单精度）。边缘偏移。

ZOffset。Float（单精度）。ZOffset.  

## Outputs

Landmass Falloff设置。Landmass Falloff设置结构。

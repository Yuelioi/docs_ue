# BreakNodalOffsetPointInfo

添加一个节点，将'NodalOffsetPointInfo'分解到其成员字段中

## 图示

![]($-20221218-14405013.png)

## Inputs

节点偏移点信息。节点偏移点信息结构（通过参考）。  

## Outputs

节点偏移。节点偏移结构。节点偏移：。节点参数。

焦点。Float（单精度）。焦点：。点聚焦值。

缩放。Float（单精度）。缩放:.点的缩放值。

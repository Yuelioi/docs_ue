# BreakSequenceLengthChangedPayloa-

添加一个打破'SequenceLengthChangedPayload'的节点到其成员字段中

## 图示

![]($-20221218-14422648.png)

## Inputs

序列长度改变的有效载荷。序列长度改变后的有效载荷结构（按参考）。  

## Outputs

以前的长度。Float (single-precision).上一个长度：。模型的上一个可播放长度。

T0：浮点数（单精度）。T0:.长度发生变化的时间。

T1:浮点数（单精度）。T1:.从T0开始插入或移除的时间长度。

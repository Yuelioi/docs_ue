# BreakARImageUpdatePayload

添加一个节点，将'ARImageUpdatePayload'分解到其成员字段中

## 图示

![]($-20221218-14310968.png)

## Inputs

ARImage Update Payload。ARImage Update Payload结构（通过引用）。 

## Outputs

Session Payload: ARSession Payload结构。Session Payload: 会话有效载荷结构。

World Transform: Transform. 世界转换。

检测到的图像。ARCandidate图像对象参考。检测到的图像。

估计的尺寸。矢量2D结构。估计的尺寸。

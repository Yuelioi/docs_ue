# BreakDMXImportGDTFDMXMode

添加一个节点，将'DMXImportGDTFDMXMode'分解到其成员字段中。

## 图示

![]($-20221218-14350438.png)

## Inputs

DMXImport GDTFDMXMode。DMXImport GDTFDMXMode 结构（通过引用）。 

## Outputs

名称。名称。名称。

Geometry（几何）。名称。几何图形。

DMX通道。DMXImport GDTFDMXChannel结构的阵列。DMX通道: DMXChannels。

关系。DMXImport GDTFRelation结构的数组。关系。

FTMacros: DMXImport GDTFFTMacro结构的数组。FTMacros.

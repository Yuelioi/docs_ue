# BreakInt32Range

添加一个节点，将'Int32Range'分解成其成员字段

## 图示

![]($-20221218-14383092.png)

## Inputs

Int 32Range:Int 32Range结构（通过引用）。  

## Outputs

下限。Int 32Range Bound结构。Lower Bound:.保存范围的下限。

上边界。Int 32Range Bound结构。Upper Bound:.保存范围的上界。

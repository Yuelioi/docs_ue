# BreakWaterBodyWeightmapSettings

添加一个节点，将'WaterBodyWeightmapSettings'分解成其成员字段

## 图示

![]($-20221218-14454002.png)

## Inputs

水体重量图设置。水体重量图设置结构（按参考）。  

## Outputs

漂移宽度：浮点数（单精度）。衰减宽度。

边缘偏移。浮点数（单精度）。边缘偏移。

调制纹理。纹理2D对象参考。调制纹理。

纹理拼接。Float（单精度）。纹理平铺。

纹理影响。Float（单精度）。纹理影响。

中点。Float (single-precision).中点。

最终不透明度。Float（单精度）。最终不透明度。

# MakeSubmixEffectDynamicProcessor-

添加一个节点，从其成员中创建一个 "SubmixEffectDynamicProcessorFilterSettings"。

## 图示

![]($-20221218-15024243.png)

## Inputs

已启用。布尔型。启用：。是否启用了过滤器。

截止时间(Hz)。Float (single-precision).切断：。应用于关键信号的HPF的截止频率。

增益（D B）。Float (single-precision).增益Db：。应用于按键信号的滤波器架的增益。  

## Outputs

Submix Effect动态处理器滤波器设置。子混音效果的动态处理器滤镜设置结构。

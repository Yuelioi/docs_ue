# BreakInputAxisKeyMapping

添加一个节点，将'InputAxisKeyMapping'分解成其成员字段

## 图示

![]($-20221218-14382389.png)

## Inputs

输入轴键的映射。输入轴键映射结构（按参考）。  

## Outputs

轴的名称。名称。轴名：。轴的友好名称，例如 "MoveForward"。

规模。Float（单精度）。规模：。累积轴值时用于映射的乘数。

钥匙。钥匙结构。钥匙：。钥匙，将其绑定到.

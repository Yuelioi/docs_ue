# BreakSourceEffectFoldbackDistort-

添加一个节点，将'SourceEffectFoldbackDistortionSettings'分解到其成员字段中

## 图示

![]($-20221218-14432927.png)

## Inputs

源效果折返失真设置。源效果折返失真设置结构（按参考）。  

## Outputs

输入增益Db。Float (single-precision).输入增益Db:。要加到输入的增益量，以允许强制触发阈值。

阈值Db。Float (single-precision).阈值Db:。如果音频振幅高于这个，就会折返。

输出增益Db。Float（单精度）。输出增益Db:。应用于输出的增益量。

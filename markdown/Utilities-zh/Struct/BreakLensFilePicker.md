# BreakLensFilePicker

添加一个节点，将'LensFilePicker'分解成其成员字段

## 图示

![]($-20221218-14390009.png)

## Inputs

镜头式文件选取器。镜头文件选取器结构（通过参考）。  

## Outputs

使用默认镜头文件：布尔值。使用默认镜头文件：。如果为真，将使用默认的LensFile，如果有设置的话。

镜头文件：镜头文件对象参考。镜头文件:。如果不需要DefaultLensFile，则使用LensFile资产。

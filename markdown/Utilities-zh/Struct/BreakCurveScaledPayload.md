# BreakCurveScaledPayload

添加一个节点，将'CurveScaledPayload'分解成其成员字段

## 图示

![]($-20221218-14333107.png)

## Inputs

曲线缩放的有效载荷。曲线缩放的载荷结构（通过引用）。 

## Outputs

因子。Float（单精度）。因数：。曲线被缩放的系数。

原点。Float（单精度）。原点:。缩放曲线时用作原点的时间。

Identifier（标识符）。Animation Curve Identifier结构。Identifier:.标识符。曲线的标识符。

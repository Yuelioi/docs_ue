# BreakRemoteControlOptionalExpose-

添加一个节点，将'RemoteControlOptionalExposeArgs'分解到其成员字段中。

## 图示

![]($-20221218-14414427.png)

## Inputs

遥控可选暴露参数。遥控可选暴露参数结构（通过参考）。  

## Outputs

显示名称。字符串。显示名称：。暴露在面板中的实体的显示名称。

集团名称。字符串。组名：。要公开实体的组的名称。如果它不存在，将创建一个具有该名称的组。

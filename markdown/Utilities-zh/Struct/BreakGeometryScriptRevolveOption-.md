# BreakGeometryScriptRevolveOption-

添加一个节点，将'GeometryScriptRevolveOptions'分解成其成员字段

## 图示

![]($-20221218-14373019.png)

## Inputs

Geometry Script Revolve Options:Geometry Script Revolve Options结构（按参考）。  

## Outputs

旋转度数。Float（单精度）。旋转度数。

度数偏移。Float（单精度）。程度偏移。

反转方向。布尔型。反转方向。

硬规范。布尔型。硬法线。

硬法线角度。Float（单精度）。硬法线角度。

中点轮廓：布尔值。在中点的轮廓。

填充部分旋转端盖。布尔型。填充部分旋转端盖。

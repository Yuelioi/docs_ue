# MakeInputClampConstants

添加一个节点，从其成员中创建一个 "InputClampConstants"。

## 图示

![]($-20221218-14555058.png)

## Inputs

钳制结果。布尔型。钳制结果。

Interp结果。布尔型。Interp结果。

夹子最小值。浮点数（单精度）。钳制最小值。

钳子最大值：浮点（单精度）。钳子的最大值。

Interp速度递增。Float（单精度）。Interp速度增加。

Interp速度递减。Float（单精度）。间歇期速度递减。  

## Outputs

输入钳制常数。输入钳制常数结构。

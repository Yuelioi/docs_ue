# MakeDisplayClust-_19

添加一个节点，从其成员中创建一个 "DisplayClusterConfigurationViewport_ColorGradingWhiteBalanceSettings"。

## 图示

![]($-20221218-14503518.png)

## Inputs

温度类型。ETemperatureMethod Enum。温度类型：。选择温度计算的类型。白平衡使用温度值来控制虚拟摄像机的白平衡。这是默认选择。色温使用温度值来调整场景的色温，这是白平衡操作的反面。

温度：浮点（单精度）。白色温度：。白色温度。

色调。Float（单精度）。White Tint:.白色的色调。  

## Outputs

Display Cluster Configuration Viewport Color Grading White Balance Settings:显示集群配置 视口颜色分级 白平衡设置 结构。

# MakeBlackboardKeySelector

添加一个节点，从其成员中创建一个 "BlackboardKeySelector"。

## 图示

![]($-20221218-14474597.png)

## Inputs

允许的类型。黑板关键类型对象引用的数组。Allowed Types:.具有额外属性的允许类型数组（例如uobject的基类）。EditAnywhere对于FBlackboardSelectorDetails::CacheBlackboardData()是必需的。

选定的密钥名称。名称。选定的密钥名称：。选定的密钥的名称。

选定的钥匙类型。黑板键类型类参考。选定的键类型：。选定的键的类别。

选定的密钥ID： 字节。选定的密钥ID：。所选密钥的ID。

无是允许的值。布尔值。无 允许的值。  

## Outputs

黑板键选择器。黑板键选择器的结构。

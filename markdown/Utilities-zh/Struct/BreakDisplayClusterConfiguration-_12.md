# BreakDisplayClusterConfiguration-_12

添加一个打破'DisplayClusterConfigurationPostRender_Override'的节点到其成员字段中

## 图示

![]($-20221218-14342930.png)

## Inputs

Display Cluster Configuration Post Render Override（显示集群配置后渲染覆盖）。Display Cluster Configuration Post Render Override（显示集群配置后渲染覆盖）结构（通过引用）。 

## Outputs

启用视口纹理替换。布尔值。允许替换：。禁用默认渲染，并将SourceTexture解析为视口。

Source Texture（源纹理）。纹理的2D对象参考。Source Texture:（源纹理）。用来代替内部地壳的纹理。

Use Texture Crop：布尔值。Should Use Texture Region:（应使用纹理区域）。设置为 "True"，以便按照下面的规定裁剪内围的纹理。

Texture Crop（纹理裁剪）。Display Cluster Replace Texture Crop Rectangle Structure。Texture Region:.纹理区域。纹理裁剪。

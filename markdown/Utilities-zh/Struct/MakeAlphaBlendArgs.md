# MakeAlphaBlendArgs

添加一个节点，从其成员中创建一个 "AlphaBlendArgs"。

## 图示

![]($-20221218-14463824.png)

## Inputs

自定义曲线。曲线浮动对象参考。自定义曲线：。如果你使用Custom BlendOption，你可以指定曲线。

混合时间。Float（单精度）。混合时间：。混合时间。

混合选项。EAlphaBlendOption Enum.Blend Option（混合选项）：。使用的混合类型（线性、立方体等）。  

## Outputs

Alpha Blend Args:Alpha Blend Args结构。

# MakeSoundControlModulationInput

添加一个节点，从其成员中创建一个 "SoundControlModulationInput"。

## 图示

![]($-20221218-15014459.png)

## Inputs

采样和保持：布尔型。采样和保持：。在父补丁初始化时获得调制的输入值，并在其生命周期内保持该值。

总线：声音控制总线对象参考。总线：。输入总线。  

## Outputs

声控调制输入。声控调制输入结构。

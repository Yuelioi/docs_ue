# BreakMappingQueryIssue

添加一个节点，将'MappingQueryIssue'分解成其成员字段

## 图示

![]($-20221218-14393999.png)

## Inputs

映射查询问题。映射查询问题结构（按参考）。  

## Outputs

问题。EMappingQueryIssue Enum.问题。

封锁语境。输入映射语境对象参考。阻断上下文：。输入上下文，包含与被查询键绑定的阻断动作。

阻断动作。输入行动对象参考。阻塞动作：。导致阻塞的输入环境中的动作。

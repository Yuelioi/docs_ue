# BreakDMXImportGDTFChannelFunctio-

Adds a node that breaks a 'DMXImportGDTFChannelFunction' into its member fields

## 图示

![]($-20221218-14350086.png)

## Inputs

DMXImport GDTFChannel Function: DMXImport GDTFChannel Function Structure (by ref).  

## Outputs

Name: Name. Name.

Attribute: DMXImport GDTFAttribute Structure. Attribute.

Original Attribute: String. Original Attribute.

DMXFrom: DMXImport GDTFDMXValue Structure. DMXFrom.

DMXValue: DMXImport GDTFDMXValue Structure. DMXValue.

Physical From: Float (single-precision). Physical From.

Physical To: Float (single-precision). Physical To.

Real Fade: Float (single-precision). Real Fade.

Wheel: DMXImport GDTFWheel Structure. Wheel.

Emitter: DMXImport GDTFEmitter Structure. Emitter.

Filter: DMXImport GDTFFilter Structure. Filter.

DMXInvert: EDMXImportGDTFDMXInvert Enum. DMXInvert.

Mode Master: String. Mode Master.

Mode From: DMXImport GDTFDMXValue Structure. Mode From.

Mode To: DMXImport GDTFDMXValue Structure. Mode To.

Channel Sets: Array of DMXImport GDTFChannel Set Structures. Channel Sets.

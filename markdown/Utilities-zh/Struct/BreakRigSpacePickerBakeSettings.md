# BreakRigSpacePickerBakeSettings

添加一个节点，将'RigSpacePickerBakeSettings'分解成其成员字段

## 图示

![]($-20221218-14420886.png)

## Inputs

钻机空间选取器烘烤设置。钻机空间选取器烘烤设置结构（按参考）。  

## Outputs

目标空间。钻机元素关键结构。目标空间。

开始帧。帧号结构。开始帧。

终端框架。框架编号结构。末端框架。

减少键。Boolean.缩减键。

误差。Float (single-precision).公差。

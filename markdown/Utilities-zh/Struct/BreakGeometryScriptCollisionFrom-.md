# BreakGeometryScriptCollisionFrom-

添加一个节点，将'GeometryScriptCollisionFromMeshOptions'分解到其成员字段中。

## 图示

![]($-20221218-14364991.png)

## 输入

Geometry Script Collision from Mesh Options: Geometry Script Collision From Mesh Options结构(通过引用)。 

## Outputs

Emit Transaction: Boolean. Emit Transaction（提交交易）。

方法。EGeometryScriptCollisionGenerationMethod Enum. 方法。

Auto Detect Spheres: Boolean. Auto Detect Spheres（自动检测球体）。

Auto Detect Boxes（自动检测盒子）。Boolean（布尔）。自动检测箱体。

Auto Detect Capsules（自动检测胶囊）。布尔值。自动检测胶囊。

最小厚度。浮点（单精度）。最小厚度。

简化船体。布尔值。简化船体。

凸面体目标面计数。整数。凸面船体目标面数。

扫除船体简化公差。Float（单精度）。扫地壳的简化公差。

Swept Hull Axis: EGeometryScriptSweptHullAxis枚举。Swept Hull Axis: EGeometryScriptSweptHullAxis枚举。

删除完全包含的形状。布尔值。删除完全包含的形状。

最大形状数。整数。最大形状数。

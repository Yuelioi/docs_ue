# MakeGeometryScriptSpatialQueryOp-

添加一个节点，从其成员中创建一个'GeometryScriptSpatialQueryOptions'。

## 图示

![]($-20221218-14543551.png)

## Inputs

最大距离。Float（单精度）。最大距离。

允许不安全的修改查询。布尔值。允许不安全的修改查询。

绕组等效阈值。Float（单精度）。绕组等效阈值。  

## Outputs

Geometry Script Spatial Query Options:BVH查询选项结构。

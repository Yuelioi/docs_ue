# MakeBrushEffectTerracing

添加一个节点，从其成员中创建一个 "刷子效果梯度"。

## 图示

![]($-20221218-14475701.png)

## Inputs

Terrace Alpha。Float (single-precision).梯田Alpha。

台阶间距。Float（单精度）。梯田间距。

露台平滑度。Float（单精度）。梯田平滑度。

掩码长度。浮点数（单精度）。掩码长度。

掩码起始偏移。Float (single-precision).掩码开始偏移。  

## Outputs

刷子效果的梯田。刷子效果的梯田结构。

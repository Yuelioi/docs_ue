# BreakDMXFixtureMode

添加一个节点，将'DMXFixtureMode'分解成其成员字段

## 图示

![]($-20221218-14345647.png)

## Inputs

DMXFixture Mode。DMXFixture模式结构（通过引用）。 

## Outputs

模式名称。字符串。模式名称。

函数。DMXFixture功能结构的阵列。功能: 功能。

通道跨度。整数。通道跨度：。该模式的功能所使用的通道（字节）的数量。

启用的夹具矩阵。布尔值。Fixture Matrix Enabled（启用的夹具矩阵）。

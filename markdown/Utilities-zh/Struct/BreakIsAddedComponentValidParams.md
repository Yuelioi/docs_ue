# BreakIsAddedComponentValidParams

添加一个节点，将'IsAddedComponentValidParams'分解到其成员字段中。

## 图示

![]($-20221218-14384934.png)

## Inputs

是否添加了组件的有效参数。是否添加了组件的有效参数结构（通过参考）。  

## Outputs

增加了组件。Actor组件对象参考。添加的组件：。这个组件被添加到演员身上。这是在编辑器世界中的一个实例。

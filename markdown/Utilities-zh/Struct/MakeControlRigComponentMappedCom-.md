# MakeControlRigComponentMappedCom-

添加一个节点，从其成员中创建一个 "ControlRigComponentMappedComponent"。

## 图示

![]($-20221218-14492553.png)

## Inputs

组件。场景组件对象参考。组件。

元素名称：名称。元素名称。

元素类型。ERigElementType枚举。元素类型：。这个元素的类型被映射到。

方向。EControlRigComponentMapDirection Enum。方向。  

## Outputs

控制钻机组件映射的组件。映射的组件结构。

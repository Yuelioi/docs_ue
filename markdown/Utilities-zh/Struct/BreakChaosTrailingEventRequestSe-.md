# BreakChaosTrailingEventRequestSe-

添加一个节点，将'ChaosTrailingEventRequestSettings'分解到其成员字段中。

## 图示

![]($-20221218-14322803.png)

## Inputs

Chaos Trailing Event Request Settings（混沌追踪事件请求设置）。混沌追踪事件请求设置结构（通过引用）。 

## Outputs

Max Number Of Results: 整数。Max Number Of Results:.最大结果数。要返回的最大结果数。

最小质量。Float（单精度）。最小质量：。结果的最小质量阀值。

最小速度。浮点（单精度）。最小速度：。结果的最小速度阈值。

最小角速度。Float (single-precision). 最小角速度：。结果的最小角速度阈值。

最大距离。Float（单精度）。最大距离：。结果的最大距离阈值（如果破坏事件监听器上设置了位置）。

排序方法。EChaosTrailingSortMethod Enum。Sort Method:.排序方法。用来对破坏事件进行排序的方法。

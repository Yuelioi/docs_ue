# BreakDisplayClusterConfiguration-_15

添加一个节点，将'DisplayClusterConfigurationProjection'分解成其成员字段

## 图示

![]($-20221218-14343192.png)

## Inputs

显示群集配置投影。显示集群配置投影结构（通过引用）。 

## Outputs

类型。字符串。类型:。多态的实体类型。

参数。字符串到字符串的映射。参数:。通用参数映射。

# BreakDMXInputPortReference

添加一个节点，将'DMXInputPortReference'分解成其成员字段

## 图示

![]($-20221218-14352630.png)

## Inputs

DMXInput Port Reference。DMX输入端口参考结构（通过参考）。 

## Outputs

端口指南。指南结构。端口指南：。与端口配置和端口实例共享的唯一标识符。注意：这需要BlueprintReadWrite，以便对属性类型的定制进行访问，但通过定制隐藏。

Enabled Flag:布尔值。Enabled Flag:.启用标志。端口引用的可选标志，可以启用或禁用。

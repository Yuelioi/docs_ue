# MakeXRGestureConfig

添加一个节点，从其成员中创建一个 "XRGestureConfig"。

## 图示

![]($-20221218-15050293.png)

## Inputs

点选：布尔值。轻敲。

保持：布尔型。保持。

轴位手势。ESpatialInputGestureAxis Enum.Axis Gesture.

导航轴X。布尔值。导航轴X。

导航轴Y：布尔值。导航轴Y。

导航轴Z：布尔值。导航轴Z。

## Outputs

XRGesture配置。XRGesture配置结构。

# MakeGeometryScriptWeldEdgesOptio-

添加一个节点，从其成员中创建一个 "GeometryScriptWeldEdgesOptions"。

## 图示

![]($-20221218-14544583.png)

## Inputs

误差。浮点数（单精度）。公差：。如果两对端点顶点和它们的中点都比这个距离近，那么边就是重合的。

只有唯一的一对。布尔型。只有唯一的配对：。只合并有唯一重复边缘匹配的无歧义对。  

## Outputs

几何脚本焊接边缘选项。Geometry Script Weld Edges Options（几何脚本焊接边缘选项）结构。

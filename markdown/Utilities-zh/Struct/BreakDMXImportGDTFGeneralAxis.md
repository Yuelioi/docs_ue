# BreakDMXImportGDTFGeneralAxis

添加一个节点，将'DMXImportGDTFGeneralAxis'分解到其成员字段中。

## 图示

![]($-20221218-14351401.png)

## Inputs

DMXImport GDTFGeneral Axis: DMXImport GDTFGeneral轴结构（通过引用）。 

## Outputs

Axis: DMXImport GDTFType轴结构的阵列。轴。

名称。名称。名称。

模型。名称。模型。

位置。矩阵结构。职务：职务。

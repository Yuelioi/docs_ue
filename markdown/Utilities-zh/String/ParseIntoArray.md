# ParseIntoArray

从源字符串中获取一个由分隔符分割的字符串数组，空字符串可以选择性地被剔除。

目标是Kismet字符串库

## 图示

![]($-20221218-14294661.png)

## Inputs

源字符串。字符串。要分割的字符串。

分隔符。字符串。要定界的字符串。

Cull Empty Strings: Boolean. = true - 剔除（true）空字符串或将其添加到数组中（false）。 

## Outputs

返回值。字符串数组。已被分离的字符串数组。

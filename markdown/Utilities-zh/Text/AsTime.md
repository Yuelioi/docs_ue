# AsTime

将传入的日期和时间转换为文本，格式为使用不变的时区的时间。这将使用给定的日期和时间，所以假定它已经在正确的时区。

目标是Kismet文本库

## 图示

![]($-20221218-15240239.png)

## Inputs

在。日期时间结构（按参考）。  

## Outputs

返回值。文本。将传入的日期和时间转换为文本，格式为使用不变的时区的时间。这将使用给定的日期和时间，所以假定它已经在正确的时区。

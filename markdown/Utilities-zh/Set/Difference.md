# Difference

将结果分配给两个集合A和B的相对差值，也就是说，结果将包含所有在集合A中但在集合B中找不到的元素。 注意，两个集合之间的差值不是交换性的。你想保留的元素的集合应该是第一个（顶层）参数。也叫相对补数。

目标是蓝图集合库

## 图示

![]($-20221218-14290667.png)

## Inputs

在。执行。

A：通配符集。开始集。

B：通配符集。要从A集合中移除的元素集合。 

## Outputs

输出。执行。

结果。通配符集。包含A中所有在B中找不到的元素的集合。
